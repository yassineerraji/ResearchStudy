# Template for the local .env file the LLM policy reads its provider
# credentials from. Copy this file to .env and fill in real values there;
# .env itself is git-ignored and must never be committed. This template
# holds no secrets and is intentionally safe to check into version control.

OPENAI_API_KEY=
LLM_MODEL=
