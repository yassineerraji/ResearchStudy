# Supply-Chain Agent Evaluation — Build Contract

> **Project:** `supply-chain-agent-evaluation`
> **Primary language:** Python
> **Implementation style:** small modular monolith
> **Core principle:** build the smallest correct system that can answer the research question fairly and reproducibly

## Document status: two parts

This file is now in **two parts**, kept in one document deliberately so V2 is always read against what it extends.

- **Part 1 — Version 1 Build Contract** (immediately below). **Complete, built, tested, and frozen.** All nine V1 milestones are done; 323 automated tests pass; the real 100-replication-per-profile experiment (Light/Medium/Heavy) has been run against the live OpenAI API and the results audited. Part 1's content is left exactly as it was written for V1 and is **not modified by this revision** — every formula, schema, file contract, and locked decision in Part 1 still describes exactly what is built and must keep working. Part 1 is reference, not a to-do list.
- **Part 2 — Version 2 Build Contract** (appended at the end of this file, after Part 1's section 37). This is the new, active contract: it extends Part 1's already-built architecture with the three axes of improvement identified from auditing V1's results — richer randomness, a topology × severity experimental grid, and an expanded, more realistic disruption taxonomy. Part 2 assumes Part 1 exists and works; it does not restate what does not change.

**Cross-reference convention:** anywhere either part needs to point at the other, it says so explicitly — "V1 §12.1" or "V2 §3" — never a bare section number. Section numbers restart at 1 inside Part 2 under its own heading; they are not a continuation of Part 1's 1–37.

**Instruction hierarchy (applies to both parts, per V1 §1.2):** Yassine's current explicit instruction for the active task still outranks either part's own invariants. When working on V2, V2's invariants are the relevant ones to check against; Part 1's invariants remain authoritative for anything V2 does not explicitly change. When working on a V1 bug fix, Part 1 remains authoritative and V2's not-yet-built mechanics are irrelevant. A lower-level source must never silently override a higher-level one, and a newly discovered ambiguity in either part must be raised, not guessed at.

---

# Part 1 — Version 1 Build Contract

This document defines, in plain language and implementation-level detail, how the first complete version of the Supply-Chain Agent Evaluation project must be built. It is the technical contract for the repository: a coding agent should be able to implement the project from this file without inventing missing domain rules, changing the research design, or introducing unnecessary architecture.

---

## 1. Authority, purpose, and change control

### 1.1 Purpose of this file

This file specifies:

- the research question and exact scope of Version 1;
- the formal supply-chain and experimental model;
- every implementation choice required to make the model executable;
- the exact repository structure;
- the responsibility and public contract of every file;
- all configuration schemas and baseline values;
- the daily simulation sequence;
- shipment, inventory, demand, disruption, routing, cost, and policy semantics;
- the heuristic decision rule;
- the bounded LLM-agent protocol;
- the paired experimental procedure;
- every required output and metric;
- the testing strategy;
- the implementation sequence;
- the acceptance criteria and definition of done.

This is not an aspirational roadmap. It is the build contract for Version 1.

### 1.2 Instruction hierarchy

When implementing the project, apply instructions in this order:

1. Yassine's current explicit instruction for the active task.
2. The scientific invariants and formal semantics in this file.
3. The repository-wide workflow and coding rules in `CLAUDE.md`.
4. Existing tests and documented public interfaces.
5. Existing implementation details.

A lower-level source must never silently override a higher-level source.

If an explicit request appears to violate a scientific invariant, stop before modifying code and explain the conflict to Yassine. Do not guess which research meaning should be changed.

### 1.3 Read-only contract

Do not edit this file during ordinary implementation tasks. Modify it only when Yassine explicitly asks to revise the implementation contract.

Do not reinterpret vague code behavior as permission to change this specification. When code and this file disagree, treat the code as incorrect unless Yassine explicitly approves a specification change.

### 1.4 Version 1 has no unresolved design questions

All decisions necessary to build Version 1 are fixed below. A coding agent must not create new behavior simply because a different design might also be reasonable.

Any newly discovered ambiguity must be treated as one of the following:

- a contradiction in this contract;
- missing source data;
- an implementation defect;
- a proposal for Version 2.

Raise it before changing formal semantics, public schemas, cost definitions, event order, fairness rules, or architecture.

---

## 2. Project mission

Build a research-grade Python simulator that compares two supply-chain disruption-response policies:

1. a transparent classical heuristic;
2. a bounded LLM-enabled decision agent.

The project must answer:

> Under identical disrupted supply-chain conditions and operational constraints, can a bounded LLM-enabled policy produce valid shipment-level decisions that reduce the incremental cost of disruption more effectively and robustly than a classical heuristic?

The system must compare policies, not give one policy a more favorable world.

---

## 3. Version 1 scope

### 3.1 What Version 1 is

Version 1 is a discrete-time, single-product, transportation-and-inventory simulator.

It represents:

- a directed logistics network;
- suppliers, ports, hubs, and one demand destination;
- shipments released according to a fixed replenishment schedule;
- inventory at the destination;
- stochastic daily demand;
- ordinary transport delays;
- temporary network disruptions;
- shipment-level decisions;
- route changes and emergency transport;
- service and cost consequences;
- paired disrupted and undisrupted experiments.

Time advances in whole days.

### 3.2 What policies control

Policies may decide only what to do with an existing shipment that is waiting at a node and requires intervention.

The shared actions are:

- `WAIT`;
- `REROUTE`;
- `EXPEDITE`;
- `ABSTAIN`.

Policies do not create supply, change customer demand, modify the network, or bypass validation.

### 3.3 Explicit non-goals

Version 1 does not include:

- supplier selection;
- procurement decisions;
- production planning;
- manufacturing bills of material;
- multiple products;
- product substitution;
- shipment splitting;
- partial expedition;
- joint optimization across multiple shipments;
- new order creation by a policy;
- dynamic pricing;
- vehicle-level scheduling;
- fleet routing;
- carbon-emission objectives;
- human approval workflows;
- free-form news interpretation;
- uncertain disruption duration;
- delayed or asymmetric information;
- reinforcement learning;
- policy training or fine-tuning;
- a database;
- a web application;
- distributed execution;
- microservices;
- asynchronous simulation;
- real-time operation.

These may be studied later, but they must not appear in Version 1 unless Yassine explicitly changes the contract.

---

## 4. Engineering philosophy

### 4.1 Simplicity first

Always choose the simplest correct implementation.

Prefer:

- one clear function over a framework;
- a dataclass over an elaborate object hierarchy;
- an explicit loop over hidden orchestration;
- standard-library serialization over a database;
- deterministic local calculations over an external service;
- a small number of dependencies;
- direct names matching the domain.

Do not write code for hypothetical future use.

### 4.2 Minimal code

Every class, function, file, dependency, configuration field, and abstraction must solve a current Version 1 requirement.

Do not add:

- generic repositories;
- service locators;
- dependency-injection frameworks;
- plugin systems;
- event buses;
- abstract factories;
- generic workflow engines;
- catch-all utility modules;
- compatibility layers;
- speculative extension points;
- unused interfaces;
- placeholder modules.

### 4.3 Separation of concerns

Protect this separation:

```text
Simulation physics
≠
Decision policy
≠
Experiment orchestration
≠
External LLM integration
≠
Input/output
```

- Simulation physics determines what happens after a validated action.
- A policy chooses an action from a read-only observation.
- Experiment orchestration creates fair, paired runs.
- The LLM integration communicates with the external provider.
- Input/output loads configuration and saves results.

### 4.4 Surgical changes

For every task:

- inspect the current state before editing;
- identify the smallest required file set;
- modify only the requested responsibility;
- add only directly related tests;
- preserve unrelated behavior;
- report unrelated problems instead of fixing them;
- never reformat or rewrite an entire file for a local change.

### 4.5 Goal-driven execution

Before coding, define:

- the goal;
- the allowed scope;
- what “done” means;
- the verification commands;
- any risk to scientific validity.

After coding, verify the completion condition before moving forward.

### 4.6 Scientific validity before convenience

If a convenience feature threatens fairness, reproducibility, or auditability, do not implement it.

Examples:

- never sample separate demand for each policy;
- never let the LLM call a routing function unavailable to the heuristic calculations;
- never silently repair an invalid LLM route;
- never skip a failed replication and average the rest;
- never change a cost coefficient for only one branch;
- never execute a policy against a mutated state from a prior branch.

---

## 5. Formal model

## 5.1 Base network

The normal supply chain is a directed graph:

$$
\bar{G} = (V, E)
$$

where:

- $\bar{G}$ is the immutable base network;
- $V$ is the set of nodes;
- $E$ is the set of directed transport edges.

An edge $(i, j)$ permits movement from node $i$ to node $j$. It does not imply movement from $j$ to $i$.

## 5.2 Node meaning

Each node represents one location.

Supported node types are exactly:

```text
SUPPLIER
PORT
HUB
PLANT
CUSTOMER
```

Version 1 uses `PLANT` as the final demand destination.

Each node has:

| Field | Type | Meaning |
|---|---:|---|
| `node_id` | `str` | Unique stable identifier |
| `name` | `str` | Human-readable name |
| `node_type` | enum | Operational role |
| `latitude` | `float \| None` | Optional reporting coordinate |
| `longitude` | `float \| None` | Optional reporting coordinate |
| `storage_capacity` | `int` | Maximum quantity physically waiting or stored at the node |
| `processing_capacity` | `int` | Maximum quantity allowed to depart from the node per day |
| `source_capacity` | `int` | Maximum quantity a supplier may release per day |

All capacities are non-negative integers.

For non-supplier nodes, `source_capacity` must be `0`.

## 5.3 Edge meaning

Each edge represents one transport lane.

Supported modes are exactly:

```text
ROAD
RAIL
SEA
AIR
```

Each edge has:

| Field | Type | Meaning |
|---|---:|---|
| `edge_id` | `str` | Unique stable identifier |
| `origin_node_id` | `str` | Start node |
| `destination_node_id` | `str` | End node |
| `mode` | enum | Transport mode |
| `distance_km` | `float` | Reporting distance |
| `base_lead_time_days` | `int` | Normal traversal time |
| `daily_capacity` | `int` | Maximum quantity entering the edge per day |
| `unit_transport_cost` | `float` | Cost per unit entering the edge |
| `reliability` | `float` | Probability of receiving no ordinary extra delay |
| `emergency` | `bool` | Whether the edge is an emergency option |

Validation rules:

- lead time must be at least `1`;
- capacity must be non-negative;
- cost must be non-negative;
- reliability must satisfy \(0 \leq reliability \leq 1\);
- origin and destination must exist;
- origin and destination must differ;
- duplicate `edge_id` values are forbidden;
- two nodes may have multiple lanes only if every lane has a distinct `edge_id`.

## 5.4 Current operational network

The operational graph on day $t$ is:

$$
G_t = \Phi(\bar{G}, Z_t)
$$

$G_t$ consists of the immutable base definitions plus temporary operational states.

Node operational state:

```text
available
processing_capacity_multiplier
```

Edge operational state:

```text
available
capacity_multiplier
lead_time_multiplier
cost_multiplier
```

All multipliers default to `1.0`.

The effective edge values on day $t$ are:

$$
\text{capacity}_{e,t} = \left\lfloor \text{base\_capacity}_e \times \text{capacity\_multiplier}_{e,t} \right\rfloor
$$

$$
\text{lead\_time}_{e,t} = \max \left(1, \left\lceil \text{base\_lead\_time}_e \times \text{lead\_time\_multiplier}_{e,t} \right\rceil \right)
$$

$$
\text{unit\_cost}_{e,t} = \text{base\_unit\_cost}_e \times \text{cost\_multiplier}_{e,t}
$$

The base graph must never be mutated.

## 5.5 Daily state

At day $t$:

$$
S_t = (G_t, I_t, B_t, H_t, D_t, Z_t, C_t)
$$

where:

- $G_t$: current operational network;
- $I_t$: inventory by node and product;
- $B_t$: backlog by destination and product;
- $H_t$: shipments and their positions;
- $D_t$: demand realized on day $t$;
- $Z_t$: disruptions physically active on day $t$;
- $C_t$: accumulated costs and service counters.
The code representation is `SimulationState`.

Policies never receive `SimulationState` directly.

## 5.6 Transition

$$
S_{t+1} = T(S_t, A_t, w_t)
$$

where:

- $T$ is shared deterministic simulation logic;
- $A_t$ is the validated executed action set;
- $w_t$ is the policy-independent exogenous event set;
- $S_{t+1}$ is the next state.

Given identical inputs, the transition must produce identical outputs.

## 5.7 Cost objective

For one run:

$$
J = C_{\text{transport}} + C_{\text{reroute}} + C_{\text{expedite}} + C_{\text{holding}} + C_{\text{backlog}} + C_{\text{late}} + C_{\text{terminal}}
$$

$C_{\text{terminal}}$ is zero when the drain phase clears all shipments and backlog. It is used only if unresolved state remains at the configured maximum drain day.

## 5.8 Paired disruption metric

For policy $p$, scenario $s$, and replication $r$:

$$
\text{TCD}_{p,s,r} = J^{\text{disrupted}}_{p,s,r} - J^{\text{undisrupted}}_{p,s,r}
$$

The policy comparison is:

$$
\Delta_{s,r} = \text{TCD}_{\text{LLM},s,r} - \text{TCD}_{\text{heuristic},s,r}
$$

Interpretation:

- $\Delta < 0$: LLM lower disruption cost;
- $\Delta > 0$: heuristic lower disruption cost;
- $\Delta = 0$: equal disruption cost.
---

## 6. Global conventions

### 6.1 Time

- Time is an integer number of days.
- Day `0` is the loaded initial state.
- Warm-up days are `1` through `warmup_days`.
- The evaluation phase begins on `warmup_days + 1`.
- `horizon_days` is the final day on which normal demand and shipment releases occur.
- The optional drain phase follows the horizon.
- Shock start and end days are inclusive.
- A shock ending on day `27` is removed at the start of day `28`.

### 6.2 Quantities

- Product, inventory, demand, shipment, and capacity quantities are integers.
- Shipment splitting is forbidden.
- Every shipment quantity must fit the static capacity of every edge in its configured initial route.
- Costs are Python `float` values and are rounded only when written for display.
- Internal cost calculations retain full floating-point precision.
- CSV and JSON cost outputs use six decimal places.

### 6.3 Currency and units

Version 1 uses configuration labels rather than hard-coded assumptions:

```text
quantity_unit: units
distance_unit: km
time_unit: day
currency: EUR
```

All values in one experiment must use the same units.

### 6.4 Identifiers

Identifiers must:

- be non-empty strings;
- contain only lowercase letters, digits, and underscores;
- remain stable across runs;
- be unique within their entity type.

Generated shipment identifiers use:

```text
shipment_<release_day:03d>_<sequence:03d>
```

Example:

```text
shipment_021_001
```

Generated route identifiers are the edge identifiers joined by `__`.

### 6.5 Ordering

Whenever order can affect results, use an explicit deterministic sort.

Shipment allocation order:

1. earliest due day;
2. earliest release day;
3. lexicographically smallest shipment ID.

Route tie-breaking order:

1. lowest estimated total cost;
2. earliest estimated arrival day;
3. fewest edges;
4. lexicographically smallest route ID.

Dictionary iteration order must never be relied upon for scientific behavior.

### 6.6 Numerical comparisons

Use:

```python
COST_TOLERANCE = 1e-9
OUTPUT_TIE_TOLERANCE = 0.01
```

- Algorithmic cost equality uses `1e-9`.
- Policy result ties use one currency cent.

---

## 7. Exact repository structure

The approved Version 1 structure is:

```text
supply-chain-agent-evaluation/
│
├── CLAUDE.md
├── README.md
├── pyproject.toml
├── .gitignore
├── .env.example
│
├── configs/
│   ├── networks/
│   │   └── baseline_network.yaml
│   │
│   ├── scenarios/
│   │   └── port_closure.yaml
│   │
│   ├── policies/
│   │   ├── heuristic.yaml
│   │   └── llm_agent.yaml
│   │
│   └── experiments/
│       └── baseline_comparison.yaml
│
├── data/
│   └── README.md
│
├── outputs/
│   └── .gitkeep
│
├── src/
│   └── supply_chain_simulator/
│       ├── __init__.py
│       ├── cli.py
│       │
│       ├── domain/
│       │   ├── __init__.py
│       │   ├── models.py
│       │   ├── state.py
│       │   ├── actions.py
│       │   └── events.py
│       │
│       ├── simulation/
│       │   ├── __init__.py
│       │   ├── engine.py
│       │   ├── transition.py
│       │   ├── routing.py
│       │   └── costs.py
│       │
│       ├── decisions/
│       │   ├── __init__.py
│       │   ├── observation.py
│       │   └── validator.py
│       │
│       ├── policies/
│       │   ├── __init__.py
│       │   ├── base.py
│       │   ├── heuristic.py
│       │   ├── llm_agent.py
│       │   └── fallback.py
│       │
│       ├── experiments/
│       │   ├── __init__.py
│       │   ├── event_tape.py
│       │   ├── runner.py
│       │   └── metrics.py
│       │
│       ├── integrations/
│       │   ├── __init__.py
│       │   └── llm_client.py
│       │
│       └── data_io/
│           ├── __init__.py
│           ├── loaders.py
│           └── writers.py
│
└── tests/
    ├── unit/
    │   ├── test_domain.py
    │   ├── test_loaders.py
    │   ├── test_event_tape.py
    │   ├── test_transition.py
    │   ├── test_routing.py
    │   ├── test_costs.py
    │   ├── test_validator.py
    │   └── test_heuristic.py
    │
    ├── integration/
    │   ├── test_full_simulation.py
    │   ├── test_paired_experiment.py
    │   └── test_reproducibility.py
    │
    └── fixtures/
        ├── tiny_network.yaml
        ├── tiny_scenario.yaml
        └── expected_results.json
```

Do not create additional architectural folders in Version 1.

Output-run subfolders are generated at runtime and are not architectural source folders.

---

## 8. Dependency policy and `pyproject.toml`

### 8.1 Python version

Use:

```text
Python >= 3.12, < 3.14
```

### 8.2 Runtime dependencies

Use only:

```toml
networkx
pydantic
PyYAML
openai
```

Purpose:

- `networkx`: directed-graph route enumeration;
- `pydantic`: strict configuration validation;
- `PyYAML`: configuration loading;
- `openai`: live LLM integration through the Responses API.

Use the standard library for:

- CSV;
- JSON and JSON Lines;
- hashing;
- dates;
- logging;
- random-number generation;
- statistics;
- file paths;
- environment variables;
- deep copying.

Do not add NumPy, pandas, SciPy, Typer, Click, Rich, Loguru, or a database library in Version 1.

### 8.3 Development dependencies

Use:

```toml
pytest
pytest-cov
ruff
mypy
```

### 8.4 Tool configuration

Configure in `pyproject.toml`:

- Ruff line length: `88`;
- Ruff Python target: `py312`;
- Mypy strict mode for `src`;
- Pytest test path: `tests`;
- Coverage source: `supply_chain_simulator`;
- minimum overall coverage: `90%`;
- branch coverage enabled.

Coverage percentage does not replace meaningful assertions.

---

## 9. Mandatory file documentation

Every Python file, including every `__init__.py` and every test file, begins with a module-level docstring before imports.

The docstring must explain:

1. what the file does within its folder;
2. how it contributes to the full system;
3. what it does not own when the boundary matters.

It must be understandable by a non-technical reader.

Example:

```python
"""Builds policy-facing observations from the current simulation state.

Inside the decisions package, this module selects and organizes the facts that
a decision policy is allowed to see. In the full system, it ensures that the
heuristic and LLM agent receive equivalent information before proposing an
action. It does not execute actions or mutate the simulation state.
"""
```

For YAML, TOML, Markdown, `.env.example`, and `.gitignore`, use an equivalent plain-language opening comment or paragraph.

Exceptions:

- generated result files;
- JSON, which does not support comments;
- `.gitkeep`.

---

## 10. Package dependency direction

Allowed direction:

```text
domain
  ↑
simulation and decisions
  ↑
policies
  ↑
experiments
  ↑
cli

integrations is used only by the LLM policy.
data_io may construct domain/configuration objects and serialize results.
```

Hard rules:

- `domain` imports no other project package.
- `simulation` imports `domain`, and may import read-only observation/policy protocols only where needed by the engine.
- `simulation` never imports `HeuristicPolicy` or `LLMAgentPolicy`.
- `decisions` imports `domain` and read-only routing calculations.
- `policies` imports `domain.actions`, `decisions.observation`, and approved integrations.
- `experiments` assembles policies and the simulation engine.
- `integrations` never imports simulation state.
- `data_io` does not implement simulation logic.
- circular imports are forbidden.

---

## 11. File-by-file implementation contract

## 11.1 Root files

### `README.md`

Must explain:

- the research question;
- Version 1 scope and non-goals;
- the architecture;
- installation;
- environment setup;
- configuration;
- how to validate configuration;
- how to run the baseline experiment;
- output files;
- reproducibility limits of live LLM calls;
- testing commands;
- a high-level system diagram;

It must not duplicate every detail in this contract.

### `pyproject.toml`

Owns:

- package metadata;
- dependencies;
- development dependencies;
- console/module configuration;
- Ruff;
- Mypy;
- Pytest;
- coverage.

### `.gitignore`

Must ignore:

```text
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.env
outputs/*
!outputs/.gitkeep
.DS_Store
```

Do not ignore source configurations or test fixtures.

### `.env.example`

Must contain only:

```text
OPENAI_API_KEY=
LLM_MODEL=
```

Never commit a real key.

## 11.2 `domain/models.py`

Owns immutable business definitions.

Required enums:

```python
NodeType
TransportMode
```

Required frozen dataclasses:

```python
Node
Edge
Product
NetworkDefinition
Route
```

Required contracts:

```python
@dataclass(frozen=True, slots=True)
class Node:
    node_id: str
    name: str
    node_type: NodeType
    latitude: float | None
    longitude: float | None
    storage_capacity: int
    processing_capacity: int
    source_capacity: int
```

```python
@dataclass(frozen=True, slots=True)
class Edge:
    edge_id: str
    origin_node_id: str
    destination_node_id: str
    mode: TransportMode
    distance_km: float
    base_lead_time_days: int
    daily_capacity: int
    unit_transport_cost: float
    reliability: float
    emergency: bool
```

```python
@dataclass(frozen=True, slots=True)
class Product:
    product_id: str
    name: str
    holding_cost_per_unit_day: float
    backlog_cost_per_unit_day: float
    late_penalty_per_unit_day: float
```

```python
@dataclass(frozen=True, slots=True)
class NetworkDefinition:
    nodes: dict[str, Node]
    edges: dict[str, Edge]
    products: dict[str, Product]
```

```python
@dataclass(frozen=True, slots=True)
class Route:
    route_id: str
    edge_ids: tuple[str, ...]
    node_ids: tuple[str, ...]
    contains_emergency_edge: bool
```

`NetworkDefinition` must expose read-only lookup methods but must not contain mutable simulation status.

## 11.3 `domain/state.py`

Owns mutable run state and results.

Required enums:

```python
ShipmentStatus:
    AT_NODE
    IN_TRANSIT
    DELIVERED
```

Required dataclasses:

```python
OperationalNodeState
OperationalEdgeState
Shipment
CostCounters
ServiceCounters
SimulationState
DailyMetrics
SimulationResult
```

Shipment contract:

```python
@dataclass(slots=True)
class Shipment:
    shipment_id: str
    product_id: str
    quantity: int
    origin_node_id: str
    destination_node_id: str
    release_day: int
    due_day: int
    planned_route_edge_ids: tuple[str, ...]
    next_edge_index: int
    status: ShipmentStatus
    current_node_id: str | None
    current_edge_id: str | None
    edge_entry_day: int | None
    edge_arrival_day: int | None
    reroute_count: int
    expedite_count: int
    capacity_wait_days: int
    delivered_day: int | None
```

Invariants:

- `AT_NODE` has a non-null `current_node_id` and null `current_edge_id`;
- `IN_TRANSIT` has null `current_node_id`, non-null `current_edge_id`, and non-null arrival day;
- `DELIVERED` has destination as `current_node_id`, null current edge, and non-null delivered day;
- `next_edge_index` points to the next edge in the planned route;
- delivered shipments are immutable in behavior;
- quantity never changes.

Operational states:

```python
@dataclass(slots=True)
class OperationalNodeState:
    available: bool = True
    processing_capacity_multiplier: float = 1.0
```

```python
@dataclass(slots=True)
class OperationalEdgeState:
    available: bool = True
    capacity_multiplier: float = 1.0
    lead_time_multiplier: float = 1.0
    cost_multiplier: float = 1.0
```

Cost counters:

```text
transport
reroute
expedite
holding
backlog
late
terminal
```

Service counters:

```text
total_demand_units
same_day_fulfilled_units
backlog_fulfilled_units
delivered_shipment_units
late_delivered_units
total_lateness_unit_days
decision_count
valid_action_count
invalid_action_count
abstention_count
fallback_count
wait_count
reroute_count
expedite_count
expedited_units
```

`SimulationState` contains:

```text
day
network_definition
node_operational_state
edge_operational_state
inventory
backlog
shipments
active_shock_ids
known_shock_ids
costs
service
daily_edge_used_capacity
daily_node_used_processing
pre_shock_inventory
pre_shock_backlog
```

Inventory and backlog use:

```python
dict[str, dict[str, int]]
```

The outer key is node ID; the inner key is product ID.

## 11.4 `domain/actions.py`

Owns policy actions and validation records.

Required enums:

```python
ActionType:
    WAIT
    REROUTE
    EXPEDITE
    ABSTAIN
```

```python
ReasonCode:
    CURRENT_ROUTE_BLOCKED
    REDUCE_LATENESS
    LOWER_ESTIMATED_COST
    NO_FEASIBLE_ALTERNATIVE
    INSUFFICIENT_INFORMATION
    POLICY_OUTPUT_INVALID
    TOOL_LIMIT_REACHED
```

Required dataclasses:

```python
@dataclass(frozen=True, slots=True)
class DecisionAction:
    shipment_id: str
    action_type: ActionType
    route_id: str | None
    reason_code: ReasonCode
    rationale: str
```

Rules:

- `WAIT` and `ABSTAIN` require `route_id=None`;
- `REROUTE` and `EXPEDITE` require a route ID;
- rationale maximum length is `300` characters;
- rationale is for audit only and never changes execution.

Required validation enum and dataclass:

```python
ValidationCode
ValidationResult
```

Validation codes:

```text
VALID
SHIPMENT_NOT_FOUND
SHIPMENT_NOT_AT_NODE
SHIPMENT_ALREADY_DELIVERED
ACTION_SHIPMENT_MISMATCH
ROUTE_REQUIRED
ROUTE_NOT_ALLOWED
ROUTE_NOT_FOUND
ROUTE_WRONG_ORIGIN
ROUTE_WRONG_DESTINATION
ROUTE_USES_UNAVAILABLE_COMPONENT
ROUTE_STATIC_CAPACITY_TOO_SMALL
REROUTE_USES_EMERGENCY_EDGE
EXPEDITE_HAS_NO_EMERGENCY_EDGE
INVALID_ACTION_SCHEMA
```

## 11.5 `domain/events.py`

Owns exogenous-event definitions.

Required enums:

```python
ShockType:
    NODE_CLOSURE
    EDGE_CLOSURE
    NODE_CAPACITY_REDUCTION
    EDGE_CAPACITY_REDUCTION
    EDGE_LEAD_TIME_INCREASE
    EDGE_COST_INCREASE
```

```python
TargetType:
    NODE
    EDGE
```

Required frozen dataclasses:

```python
DemandEvent
ShipmentReleaseEvent
Shock
DayEvents
EventTape
```

`DemandEvent`:

```text
day
destination_node_id
product_id
quantity
```

`ShipmentReleaseEvent`:

```text
day
shipment_id
product_id
quantity
origin_node_id
destination_node_id
due_day
initial_route_edge_ids
```

`Shock`:

```text
shock_id
shock_type
target_type
target_id
physical_start_day
physical_end_day
information_day
capacity_multiplier
lead_time_multiplier
cost_multiplier
```

Unused multiplier fields remain `1.0`.

`DayEvents`:

```text
day
demand_events
shipment_release_events
edge_extra_delay_days
newly_known_shock_ids
```

`edge_extra_delay_days` maps every edge ID to `0` or `1`.

`EventTape` contains:

```text
scenario_id
replication
seed
days
shocks
```

It must be immutable after generation.

## 11.6 `simulation/routing.py`

Owns all graph and route calculations.

Required functions:

```python
build_operational_graph(...)
route_from_edge_ids(...)
enumerate_candidate_routes(...)
estimate_route_option(...)
estimate_current_plan(...)
get_effective_edge_capacity(...)
get_effective_edge_lead_time(...)
get_effective_edge_cost(...)
```

Rules:

- use `networkx.DiGraph`;
- nodes or edges that are currently unavailable are excluded from new candidate routes;
- candidate routes are simple paths;
- maximum route length is `6` edges;
- generate candidates from:
  - the ten shortest cost-weighted simple paths;
  - the ten shortest lead-time-weighted simple paths;
- merge by route ID;
- filter invalid/static-capacity-incompatible routes;
- return at most five options after deterministic sorting;
- never reserve future capacity during route estimation;
- current-day remaining capacity is reported but does not make a structurally valid route disappear;
- future ordinary delays are unknown and excluded from estimates;
- known shock end dates may be used when estimating `WAIT`;
- route estimation has no side effects.

A route option must expose:

```text
route_id
edge_ids
node_ids
contains_emergency_edge
estimated_lead_time_days
estimated_arrival_day
estimated_lateness_days
estimated_transport_cost
estimated_action_cost
estimated_late_penalty
estimated_total_cost
first_edge_remaining_capacity
currently_dispatchable
```

Estimated total cost is:

\[
transport + action + estimated\_late\_penalty
\]

It is a decision aid, not the final simulation cost.

## 11.7 `simulation/costs.py`

Owns shared cost calculations.

Required functions:

```python
charge_edge_entry_transport_cost(...)
charge_reroute_cost(...)
charge_expedite_cost(...)
charge_end_of_day_holding_cost(...)
charge_end_of_day_backlog_cost(...)
charge_delivery_late_penalty(...)
charge_terminal_cost(...)
total_cost(...)
```

Exact rules:

### Transport

When a shipment enters an edge:

$$
\text{cost} = \text{quantity} \times \text{effective\_unit\_transport\_cost}
$$

Charge once per edge entry.

### Reroute

When a validated `REROUTE` changes the remaining route:

$$
\text{cost} = \text{quantity} \times \text{reroute\_cost\_per\_unit}
$$

Charge on every executed reroute.

### Expedite

When a validated `EXPEDITE` changes the remaining route:

$$
\text{cost} = \text{quantity} \times \text{expedite\_premium\_per\_unit}
$$

Emergency-edge transport cost is still charged normally at edge entry.

### Holding

At the end of each day:

$$
C_{\text{holding},t} = \sum_{v,p} \text{inventory}_t(v,p) \times \text{holding\_cost}_p
$$

### Backlog

At the end of each day:

$$
C_{\text{backlog},t} = \sum_{v,p} \text{backlog}_t(v,p) \times \text{backlog\_cost}_p
$$

### Late delivery

When a shipment reaches its final destination:

$$
\text{lateness} = \max(0, \text{delivered\_day} - \text{due\_day})
$$

$$
\text{late\_cost} = \text{quantity} \times \text{lateness} \times \text{late\_penalty}_p
$$

Charge once.

### Terminal

At the last drain day, if unresolved state remains:

$$
\text{terminal\_backlog} = \text{remaining\_backlog} \times \text{backlog\_cost\_per\_unit\_day} \times \text{terminal\_penalty\_days}
$$

For each undelivered shipment:

$$
\text{terminal\_shipment} = \text{quantity} \times \text{late\_penalty\_per\_unit\_day} \times \max(1, \text{last\_day} - \text{due\_day})
$$

The baseline uses `terminal_penalty_days=30`.

## 11.8 `simulation/transition.py`

Owns pure or narrowly mutating daily state changes.

Required functions:

```python
reset_daily_capacity_usage(...)
apply_shock_operational_state(...)
process_due_arrivals(...)
release_shipments(...)
fulfil_backlog_and_demand(...)
identify_shipments_requiring_decision(...)
apply_validated_actions(...)
allocate_departures(...)
charge_end_of_day_costs(...)
record_daily_metrics(...)
```

This file does not call an LLM and does not choose actions.

## 11.9 `simulation/engine.py`

Owns the daily orchestration loop.

Required protocol:

```python
class SimulationEngine:
    def run(
        self,
        initial_state: SimulationState,
        policy: Policy,
        event_tape: EventTape,
        start_day: int,
        horizon_day: int,
        drain_days: int,
        decision_enabled: bool,
        run_identity: RunIdentity,
    ) -> SimulationResult:
        ...
```

`run_identity` contains:

```text
experiment_id
scenario_id
replication
policy_name
run_kind
```

The engine:

- deep-clones the supplied initial state;
- does not mutate the caller's snapshot;
- processes days in order;
- stops early in the drain phase if all shipments are delivered and backlog is zero;
- applies terminal costs if unresolved after the maximum drain day;
- returns a complete result and decision trace;
- fails on violated state invariants.

The engine never branches on a concrete policy type.

## 11.10 `decisions/observation.py`

Owns the read-only policy view.

Required frozen dataclasses:

```python
ShipmentContext
DestinationContext
ShockContext
RouteOption
DecisionObservation
```

`DecisionObservation` contains exactly:

```text
observation_id
day
shipment
destination
relevant_shocks
current_plan
route_options
allowed_actions
```

Shipment context:

```text
shipment_id
product_id
quantity
current_node_id
destination_node_id
release_day
due_day
days_until_due
capacity_wait_days
remaining_route_edge_ids
```

Destination context:

```text
inventory_on_hand
backlog_units
mean_daily_demand
days_of_supply
```

Shock context:

```text
shock_id
shock_type
target_id
physical_start_day
physical_end_day
information_day
known_effect
```

`days_of_supply` is:

\[
inventory / mean\_daily\_demand
\]

Use `None` when mean demand is zero.

The observation builder:

- uses only information known by the current day;
- builds every observation from the same pre-action state;
- never exposes future ordinary delay draws;
- never exposes a full mutable state;
- sorts route options deterministically;
- produces a canonical JSON-serializable dictionary for hashing.

## 11.11 `decisions/validator.py`

Owns shared action validation.

Required function:

```python
validate_action(
    action: DecisionAction,
    observation: DecisionObservation,
    state: SimulationState,
) -> ValidationResult
```

Validation order:

1. action schema;
2. shipment identity;
3. shipment exists;
4. shipment is at a node;
5. shipment is not delivered;
6. route requirement;
7. route appears in the observation's approved route options;
8. route begins at the current node;
9. route ends at destination;
10. route uses currently available nodes and edges;
11. every route edge has static/effective capacity at least shipment quantity;
12. action semantics match emergency status.

Do not silently alter the action.

Current-day shared-capacity contention is resolved later by deterministic allocation, not by rejecting otherwise valid simultaneous actions.

## 11.12 `policies/base.py`

Owns common policy contracts.

Required protocol:

```python
class Policy(Protocol):
    @property
    def name(self) -> str:
        ...

    def decide(
        self,
        observation: DecisionObservation,
    ) -> DecisionAction:
        ...
```

Also define:

```python
PolicyDecisionRecord
```

It stores proposed action, timing, and policy metadata without executing anything.

## 11.13 `policies/heuristic.py`

Owns the classical benchmark.

It must be deterministic.

The exact algorithm is:

1. Construct a `WAIT` candidate from `current_plan`.
2. Construct one candidate for every non-emergency route as `REROUTE`.
3. Construct one candidate for every emergency route as `EXPEDITE`.
4. Remove `EXPEDITE` candidates unless:
   - predicted lateness under `WAIT` is at least `expedite_trigger_lateness_days`; or
   - there is no non-emergency route.
5. Compare `estimated_total_cost`.
6. Choose the lowest-cost candidate.
7. Apply tie-breaking preference:
   - `WAIT`;
   - `REROUTE`;
   - `EXPEDITE`;
   - route ID.
8. Return the structured action.

If no route exists, return `WAIT`.

The heuristic must not use hidden future events.

## 11.14 `policies/fallback.py`

Owns fallback behavior.

Required implementations:

```python
WaitFallbackPolicy
HeuristicFallbackPolicy
```

Default LLM fallback is the heuristic.

Fallback chain:

1. LLM returns `ABSTAIN` or invalid action;
2. run configured fallback on the same observation;
3. validate fallback action;
4. if fallback is invalid, execute terminal safe `WAIT`;
5. log every stage separately.

A heuristic policy run also passes through validation. If its action is invalid, terminal safe `WAIT` is used and the run records an invalid action.

## 11.15 `policies/llm_agent.py`

Owns bounded LLM-agent orchestration.

The LLM must never receive or mutate `SimulationState`.

Approved tools are exactly:

```text
get_shipment_context
get_destination_context
list_route_options
inspect_route
submit_action
```

Tool meanings:

- `get_shipment_context`: returns shipment context from the observation;
- `get_destination_context`: returns inventory, backlog, demand, and days of supply;
- `list_route_options`: returns approved route summaries;
- `inspect_route`: returns one approved route's complete estimate;
- `submit_action`: submits the final structured action.

Tools are deterministic views over one immutable observation.

No tool:

- changes state;
- creates a route;
- runs arbitrary code;
- reads files;
- accesses the internet;
- sees future event-tape values.

Agent loop:

1. Build system instructions.
2. Provide observation ID and concise decision problem.
3. Call the LLM with approved tools.
4. Execute requested read-only tools locally.
5. Append tool outputs.
6. Continue until `submit_action`.
7. Stop after `max_tool_calls`.
8. If no valid submission exists, produce `ABSTAIN` with `TOOL_LIMIT_REACHED` or `POLICY_OUTPUT_INVALID`.

The agent must submit:

```text
shipment_id
action_type
route_id
reason_code
rationale
```

The rationale is concise and limited to 300 characters.

System prompt requirements:

- objective: reduce total disruption cost while respecting feasibility;
- use only supplied facts and tools;
- never invent routes or capacities;
- choose one action;
- use `ABSTAIN` when information is insufficient;
- do not attempt to modify state;
- submit through `submit_action`;
- do not include private chain-of-thought;
- provide only a short audit rationale.

## 11.16 `integrations/llm_client.py`

Owns provider communication.

Required interface:

```python
class LLMClient(Protocol):
    def run_tool_loop(...) -> LLMInteractionResult:
        ...
```

Required implementations in the same file:

```python
OpenAIResponsesClient
ReplayLLMClient
FakeLLMClient
```

`OpenAIResponsesClient`:

- uses the official OpenAI Python SDK;
- uses the Responses API;
- uses function tools with strict JSON schemas;
- uses `store=False`;
- uses the model named by `LLM_MODEL` or policy configuration;
- uses temperature `0.0` when supported;
- has a 60-second request timeout;
- retries transient provider errors three times with waits of 1, 2, and 4 seconds;
- does not retry invalid model content;
- exposes token usage and latency when available;
- never logs API keys.

Infrastructure failure is not converted to a policy fallback. It fails the experiment because silently replacing an unavailable LLM with a heuristic would invalidate the comparison.

`ReplayLLMClient`:

- loads prior `llm_interactions.jsonl`;
- matches by deterministic `decision_key`;
- returns the recorded action and interaction;
- raises an explicit error if a key is missing or duplicated.

`FakeLLMClient` is only for tests.

## 11.17 `experiments/event_tape.py`

Owns policy-independent randomness generation.

Required functions:

```python
derive_stream_seed(...)
generate_demand_events(...)
generate_edge_delay_draws(...)
generate_shipment_release_events(...)
build_disrupted_event_tape(...)
build_undisrupted_event_tape(...)
```

Replication seed:

\[
replication\_seed=base\_seed+replication
\]

Replication numbering starts at `1`.

Independent stream seeds are derived with SHA-256:

```text
sha256(f"{replication_seed}:{stream_name}")
```

Use the first eight bytes as an unsigned integer.

Required streams:

```text
demand
edge_delays
```

Draw order within a stream is deterministic:

- days ascending;
- destination IDs ascending;
- product IDs ascending;
- edge IDs ascending.

Demand:

```python
round(random.gauss(mean, standard_deviation))
```

Then clamp to configured minimum and maximum.

Ordinary edge delay:

```text
0 days when random draw <= reliability
1 day otherwise
```

Generate a delay draw for every edge and every day, regardless of whether a policy uses that edge. This prevents policy choices from changing the random-number sequence.

The undisrupted tape is a copy with the designed shocks removed. Demand, shipment releases, and ordinary delays remain identical.

## 11.18 `experiments/runner.py`

Owns complete paired experiments.

Required class:

```python
class ExperimentRunner:
    def run(self, config: ExperimentConfig) -> ExperimentResult:
        ...
```

For every replication:

1. derive seed;
2. generate full disrupted event tape;
3. derive matching undisrupted tape by removing only designed shocks;
4. create the day-0 initial state;
5. run warm-up once with decisions disabled and without the designed shock;
6. snapshot the state after `warmup_days`;
7. reset cost and service counters while preserving inventory, backlog, shipment positions, due dates, and operationally normal network state;
8. deep-clone that snapshot four times;
9. run:
   - heuristic undisrupted;
   - heuristic disrupted;
   - LLM undisrupted;
   - LLM disrupted;
10. calculate run metrics;
11. calculate both TCD values;
12. calculate paired delta;
13. write outputs incrementally;
14. fail the full experiment if any branch fails.

The order above is fixed.

Warm-up events come from the matching undisrupted tape.

The same snapshot and event streams after warm-up are used by every branch.

## 11.19 `experiments/metrics.py`

Owns run-level and experiment-level metrics.

Required run metrics:

```text
total_cost
transport_cost
reroute_cost
expedite_cost
holding_cost
backlog_cost
late_cost
terminal_cost
same_day_fill_rate
final_fulfilment_rate
ending_backlog_units
backlog_unit_days
late_delivered_units
late_delivery_rate
average_lateness_days_weighted
reroute_count
expedite_count
expedited_units
decision_count
invalid_action_rate
abstention_rate
fallback_rate
mean_decision_latency_ms
days_to_clear_backlog_after_shock
terminated_with_unresolved_state
```

Definitions:
$$
\text{same\_day\_fill\_rate} = \frac{\text{same\_day\_fulfilled}}{\text{total\_demand}}
$$

$$
\text{final\_fulfilment\_rate} = \frac{\text{total\_demand} - \text{ending\_backlog}}{\text{total\_demand}}
$$

$$
\text{late\_delivery\_rate} = \frac{\text{late\_delivered\_units}}{\text{delivered\_shipment\_units}}
$$

$$
\text{average\_lateness} = \frac{\text{total\_lateness\_unit\_days}}{\text{late\_delivered\_units}}
$$

Rates are `0.0` when their denominator is zero.

`days_to_clear_backlog_after_shock` is the number of days from the first day after shock end to the first day backlog becomes zero. Use `null` if backlog never reaches zero.

Experiment summary across paired deltas includes:

```text
replication_count
mean_heuristic_tcd
median_heuristic_tcd
mean_llm_tcd
median_llm_tcd
mean_delta
median_delta
standard_deviation_delta
mean_delta_ci_95_lower
mean_delta_ci_95_upper
llm_win_rate
heuristic_win_rate
tie_rate
best_llm_delta
worst_llm_delta
p10_delta
p90_delta
```

The 95% confidence interval uses:

$$
\text{mean} \pm 1.96 \times \frac{\text{sample\_standard\_deviation}}{\sqrt{n}}
$$
Percentiles use deterministic linear interpolation over sorted values.

Winner:

- LLM when delta `< -0.01`;
- heuristic when delta `> 0.01`;
- tie otherwise.

Do not add a p-value in Version 1.

## 11.20 `data_io/loaders.py`

Owns configuration models, path resolution, and conversion to domain objects.

Use Pydantic models with:

```text
extra="forbid"
strict=True where practical
```

Required configuration classes:

```text
NetworkConfig
ScenarioConfig
HeuristicPolicyConfig
LLMPolicyConfig
ExperimentConfig
ResolvedConfig
```

Rules:

- paths in an experiment file are resolved relative to that experiment file;
- all referenced files must exist;
- schema version must equal `1`;
- unknown fields are errors;
- all identifiers are validated;
- network connectivity and route continuity are validated;
- the baseline replenishment route must begin at the source and end at the destination;
- warm-up must be less than horizon;
- every shock starts after warm-up in the baseline experiment;
- secrets are read only from environment variables;
- resolved configuration written to output must redact secrets.

## 11.21 `data_io/writers.py`

Owns result serialization.

Required class:

```python
class ExperimentWriter:
    ...
```

Responsibilities:

- create one output directory;
- write the manifest and resolved configuration;
- append event tapes;
- append run metrics;
- append daily metrics;
- append decisions;
- append LLM interactions;
- write replication comparisons;
- write final summary;
- flush after each replication.

Use atomic write-then-rename for complete JSON and YAML files.

Use standard `csv` and `json`.

## 11.22 `cli.py`

Owns command-line parsing and top-level error mapping.

Use `argparse`.

Commands:

```bash
python -m supply_chain_simulator.cli validate-config \
  --config configs/experiments/baseline_comparison.yaml
```

```bash
python -m supply_chain_simulator.cli run \
  --config configs/experiments/baseline_comparison.yaml
```

No command-line overrides for scientific parameters in Version 1. Change the configuration file instead so every run remains self-describing.

Exit codes:

```text
0 success
1 unexpected error
2 configuration error
3 simulation invariant error
4 LLM integration error
```

The CLI prints:

- validation result;
- experiment ID;
- replication progress;
- final output path;
- final mean delta and winner rates.

It does not implement business logic.

---

## 12. Configuration contracts

## 12.1 `configs/networks/baseline_network.yaml`

The file must contain:

```yaml
# Defines the normal logistics network, one product, initial inventory,
# replenishment schedule, demand process, and shared cost parameters used by
# the baseline experiment. It describes the world before temporary shocks.

schema_version: 1
network_id: baseline_network

units:
  quantity: units
  distance: km
  time: day
  currency: EUR

products:
  - product_id: component_a
    name: Component A
    holding_cost_per_unit_day: 0.10
    backlog_cost_per_unit_day: 3.00
    late_penalty_per_unit_day: 1.50

nodes:
  - node_id: supplier_1
    name: Primary Supplier
    node_type: SUPPLIER
    latitude: null
    longitude: null
    storage_capacity: 2000
    processing_capacity: 200
    source_capacity: 100

  - node_id: port_primary
    name: Primary Port
    node_type: PORT
    latitude: null
    longitude: null
    storage_capacity: 2000
    processing_capacity: 200
    source_capacity: 0

  - node_id: port_alternative
    name: Alternative Port
    node_type: PORT
    latitude: null
    longitude: null
    storage_capacity: 1200
    processing_capacity: 80
    source_capacity: 0

  - node_id: hub_1
    name: Import Hub
    node_type: HUB
    latitude: null
    longitude: null
    storage_capacity: 2000
    processing_capacity: 200
    source_capacity: 0

  - node_id: plant_1
    name: Manufacturing Plant
    node_type: PLANT
    latitude: null
    longitude: null
    storage_capacity: 1000
    processing_capacity: 200
    source_capacity: 0

edges:
  - edge_id: supplier_to_primary_port
    origin_node_id: supplier_1
    destination_node_id: port_primary
    mode: ROAD
    distance_km: 400
    base_lead_time_days: 2
    daily_capacity: 100
    unit_transport_cost: 4.00
    reliability: 0.97
    emergency: false

  - edge_id: primary_port_to_hub
    origin_node_id: port_primary
    destination_node_id: hub_1
    mode: SEA
    distance_km: 6000
    base_lead_time_days: 10
    daily_capacity: 200
    unit_transport_cost: 8.00
    reliability: 0.94
    emergency: false

  - edge_id: supplier_to_alternative_port
    origin_node_id: supplier_1
    destination_node_id: port_alternative
    mode: ROAD
    distance_km: 650
    base_lead_time_days: 4
    daily_capacity: 60
    unit_transport_cost: 7.00
    reliability: 0.96
    emergency: false

  - edge_id: alternative_port_to_hub
    origin_node_id: port_alternative
    destination_node_id: hub_1
    mode: SEA
    distance_km: 6700
    base_lead_time_days: 12
    daily_capacity: 60
    unit_transport_cost: 11.00
    reliability: 0.93
    emergency: false

  - edge_id: hub_to_plant
    origin_node_id: hub_1
    destination_node_id: plant_1
    mode: ROAD
    distance_km: 300
    base_lead_time_days: 2
    daily_capacity: 120
    unit_transport_cost: 3.00
    reliability: 0.98
    emergency: false

  - edge_id: supplier_to_plant_air
    origin_node_id: supplier_1
    destination_node_id: plant_1
    mode: AIR
    distance_km: 6200
    base_lead_time_days: 2
    daily_capacity: 40
    unit_transport_cost: 40.00
    reliability: 0.99
    emergency: true

initial_inventory:
  - node_id: plant_1
    product_id: component_a
    quantity: 200

demand_process:
  destination_node_id: plant_1
  product_id: component_a
  distribution: TRUNCATED_NORMAL
  mean_daily_demand: 40
  standard_deviation: 5
  minimum_daily_demand: 25
  maximum_daily_demand: 55

replenishment_plan:
  product_id: component_a
  origin_node_id: supplier_1
  destination_node_id: plant_1
  first_release_day: 1
  release_every_days: 1
  shipment_quantity: 40
  due_offset_days: 15
  initial_route_edge_ids:
    - supplier_to_primary_port
    - primary_port_to_hub
    - hub_to_plant

action_costs:
  reroute_cost_per_unit: 2.00
  expedite_premium_per_unit: 10.00
```

## 12.2 `configs/scenarios/port_closure.yaml`

```yaml
# Defines the designed disruption applied after warm-up. The primary port is
# physically unavailable for seven full days, and both policies receive the
# same complete information when the closure begins.

schema_version: 1
scenario_id: primary_port_closure
description: Complete temporary closure of the primary port.

shocks:
  - shock_id: close_primary_port
    shock_type: NODE_CLOSURE
    target_type: NODE
    target_id: port_primary
    physical_start_day: 21
    physical_end_day: 27
    information_day: 21
    capacity_multiplier: 1.0
    lead_time_multiplier: 1.0
    cost_multiplier: 1.0
```

Node-closure semantics:

- the node is unavailable from the start of day 21 through the end of day 27;
- no shipment may depart from it;
- no shipment may complete arrival into it;
- an in-transit shipment due to arrive there is postponed one day at a time;
- a shipment already waiting there remains there;
- adjacent edges are excluded from new route candidates;
- already in-transit shipments on adjacent edges are not cancelled;
- the node reopens at the start of day 28.

## 12.3 `configs/policies/heuristic.yaml`

```yaml
# Defines the transparent classical decision rule. It selects the lowest
# estimated-cost valid action and uses conservative action preferences to
# break exact ties.

schema_version: 1
policy_id: heuristic
policy_type: HEURISTIC

expedite_trigger_lateness_days: 2
cost_tolerance: 1.0e-9
```

## 12.4 `configs/policies/llm_agent.yaml`

```yaml
# Defines the bounded LLM decision agent, its approved provider settings,
# tool-call limit, fallback, and live or replay execution behavior.

schema_version: 1
policy_id: llm_agent
policy_type: LLM

provider: OPENAI
api_key_environment_variable: OPENAI_API_KEY
model_environment_variable: LLM_MODEL
execution_mode: LIVE
temperature: 0.0
max_tool_calls: 8
max_output_tokens: 1000
request_timeout_seconds: 60
max_retries: 3
store_provider_response: false
fallback_policy: HEURISTIC
replay_trace_path: null
```

`execution_mode` is exactly `LIVE` or `REPLAY`.

In `REPLAY`, `replay_trace_path` is required and provider credentials are not required.

## 12.5 `configs/experiments/baseline_comparison.yaml`

```yaml
# Combines the baseline network, port-closure scenario, and two policies into
# one paired experiment. It fixes the horizon, warm-up, drain period, seeds,
# output behavior, and statistical summary.

schema_version: 1
experiment_id: baseline_port_closure_comparison

network_config: ../networks/baseline_network.yaml
scenario_config: ../scenarios/port_closure.yaml

policy_configs:
  heuristic: ../policies/heuristic.yaml
  llm_agent: ../policies/llm_agent.yaml

warmup_days: 20
horizon_days: 60
drain_days: 30
terminal_penalty_days: 30

replications: 100
base_seed: 1042

counterfactual_mode: POLICY_SPECIFIC
fail_fast: true

output_root: ../../outputs
write_event_tapes: true
write_daily_metrics: true
write_decision_traces: true
write_llm_interactions: true
```

No relative path may escape the repository root.

---

## 13. Day-0 initial state and warm-up

### 13.1 Day 0

At day 0:

- the base network is normal;
- all operational states use defaults;
- plant inventory is 200;
- backlog is zero;
- there are no shipments;
- costs and service counters are zero;
- no shock is active or known.

### 13.2 Warm-up

Warm-up runs days 1 through 20.

Rules:

- use the undisrupted event tape;
- decisions are disabled;
- shipments follow their configured route;
- ordinary delays and capacity constraints still apply;
- demand and replenishment occur normally;
- no designed shock is active;
- the purpose is to build a realistic in-transit pipeline.

At the end of day 20:

- clone the complete physical state;
- preserve inventory, backlog, shipments, due dates, and positions;
- preserve no active designed shock;
- record pre-shock inventory and backlog;
- reset all cost and service counters to zero;
- reset daily capacity counters;
- retain the current day as 20.

This reset means evaluation costs start on day 21.

---

## 14. Exact daily event order

For each day \(t\) after the current state:

1. **Begin day**
   - set `state.day=t`;
   - reset daily edge and node usage.

2. **Reveal information**
   - add shocks whose `information_day=t` to known shocks.

3. **Apply physical shocks**
   - rebuild operational multipliers and availability from all shocks active on day \(t\);
   - do not incrementally mutate stale multipliers.

4. **Process arrivals**
   - find in-transit shipments with `edge_arrival_day=t`;
   - process in deterministic shipment order;
   - if destination node is unavailable, postpone arrival to `t+1`;
   - if destination storage would overflow, postpone arrival to `t+1`;
   - otherwise place shipment at the destination node;
   - if this is the final destination:
     - mark delivered;
     - add quantity to inventory;
     - charge late-delivery penalty;
   - otherwise mark `AT_NODE`.

5. **Release scheduled shipments**
   - create release events for day \(t\);
   - validate source availability, source capacity, and source storage;
   - baseline configuration guarantees feasibility;
   - a violated guarantee raises `SimulationInvariantError`;
   - released shipments are `AT_NODE` at the source.

6. **Realize and fulfil demand**
   - add the day demand to total demand counters;
   - use inventory first to clear existing backlog;
   - then use remaining inventory for same-day demand;
   - unmet same-day demand becomes backlog;
   - inventory and backlog remain non-negative.

7. **Build decision set**
   - consider every non-delivered shipment at a node;
   - build all observations from this same pre-action state;
   - do not execute an earlier action before building a later observation.

8. **Request policy decisions**
   - process observations in deterministic shipment order;
   - measure policy decision latency;
   - collect proposed actions.

9. **Validate and resolve fallback**
   - validate each proposed action;
   - process abstentions and invalid actions through configured fallback;
   - validate fallback;
   - use terminal `WAIT` if fallback is invalid;
   - record proposed, validation, fallback, and final action separately.

10. **Apply valid route changes**
    - apply `REROUTE` or `EXPEDITE`;
    - charge action cost;
    - update counters;
    - `WAIT` leaves the route unchanged.

11. **Allocate departures**
    - include all shipments at nodes that have a next edge;
    - process in deterministic shipment order;
    - enforce current node processing and edge entry capacities;
    - if capacity is insufficient, shipment remains at node and `capacity_wait_days += 1`;
    - if dispatched:
      - reset `capacity_wait_days=0`;
      - charge transport cost;
      - sample no randomness here;
      - read the pre-generated edge delay for edge and day;
      - calculate arrival day;
      - mark in transit;
      - update daily used capacities.

12. **Charge end-of-day costs**
    - holding cost on inventory;
    - backlog cost on remaining backlog.

13. **Record metrics and assert invariants**
    - record daily metrics;
    - validate non-negativity, shipment-state consistency, and capacity limits;
    - fail immediately on violation.

### 14.1 Edge arrival day

If a shipment enters an edge on day $t$:

$$
\text{arrival\_day} = t + \text{effective\_lead\_time} + \text{ordinary\_extra\_delay}
$$

It may not arrive on the same day it enters an edge.

### 14.2 Multi-edge movement

A shipment arriving at an intermediate node during step 4 may enter its next edge during step 11 of the same day.

---

## 15. Shipment lifecycle

Valid state transitions:

```text
AT_NODE → IN_TRANSIT
IN_TRANSIT → AT_NODE
IN_TRANSIT → DELIVERED
AT_NODE → AT_NODE
```

`AT_NODE → AT_NODE` occurs when:

- waiting by policy;
- waiting for capacity;
- current node is unavailable;
- the next edge is unavailable;
- no route exists.

A delivered shipment never moves again.

### 15.1 Remaining route

`planned_route_edge_ids` always begins conceptually at the shipment origin, but `next_edge_index` identifies the next edge to execute.

A reroute replaces only the remaining segment:

```text
completed history remains unchanged
new remaining route begins at current_node_id
new remaining route ends at destination_node_id
next_edge_index resets to 0 for the replacement tuple
```

To avoid ambiguous indexing, after a reroute `planned_route_edge_ids` becomes only the new remaining route, and `next_edge_index=0`. Completed route history is retained only in decision/audit traces, not in the active route tuple.

### 15.2 Storage occupancy

Node occupied quantity is:

```text
inventory stored at node
+ quantities of non-delivered shipments currently AT_NODE
```

A shipment in transit does not occupy node storage.

The final plant inventory includes delivered shipment quantity.

---

## 16. Demand and backlog semantics

Version 1 has one product and one demand destination.

Daily fulfilment order:

1. oldest aggregate backlog;
2. current-day demand.

Backlog is aggregated by destination and product. Version 1 does not track individual customer orders.

Backlog-unit-days are the sum of end-of-day backlog quantities over evaluation and drain days.

No demand is generated after `horizon_days`.

---

## 17. Shipment release semantics

The replenishment plan releases one shipment every day from day 1 through day 60 inclusive.

Each shipment:

- contains 40 units;
- begins at `supplier_1`;
- ends at `plant_1`;
- uses the primary route;
- has due day `release_day + 15`.

No new shipment is released during the drain phase.

The policy cannot cancel, duplicate, resize, or create a shipment.

---

## 18. Shock semantics

### 18.1 Information and physical effects

`information_day` controls what policies know.

`physical_start_day` and `physical_end_day` control the network.

Version 1 baseline uses:

```text
information_day = physical_start_day
```

The full end date and operational effect are known when revealed.

### 18.2 Supported effects

#### Node closure

- node unavailable;
- no arrivals complete;
- no departures;
- adjacent edges not available for new route candidates.

#### Edge closure

- no new entries;
- shipments already in transit continue using their existing arrival day.

#### Node capacity reduction

- multiply daily departure processing capacity;
- node remains available.

#### Edge capacity reduction

- multiply edge entry capacity;
- in-transit shipments unaffected.

#### Edge lead-time increase

- applies only to shipments entering while active;
- in-transit shipments unaffected.

#### Edge cost increase

- applies only to edge entries while active.

### 18.3 Overlapping shocks

Overlapping effects combine as follows:

- availability is logical AND;
- capacity multipliers are multiplied;
- lead-time multipliers are multiplied;
- cost multipliers are multiplied.

All active shocks are applied from the immutable base values every day. Never multiply yesterday's already modified value.

---

## 19. Decision-trigger rules

A shipment requires a policy decision only when all are true:

- status is `AT_NODE`;
- current node is not the final destination;
- at least one trigger below holds.

Triggers:

1. a known active shock affects the current remaining route;
2. the next edge or next node is currently unavailable;
3. estimated arrival under the current route is after the due day;
4. `capacity_wait_days >= 2`;
5. the active route is malformed or no longer continuous.

If no trigger holds, the shipment continues automatically on its route.

A shipment may require decisions on multiple days.

No decision is made while a shipment is in transit.

---

## 20. Observation and information fairness

Every policy receives the same `DecisionObservation`.

The heuristic receives it as a Python object.

The LLM accesses only that object's fields through the prompt and tools.

The LLM does not receive:

- future random demand;
- future ordinary delays;
- hidden policy outcomes;
- another policy's decisions;
- final costs;
- mutable state;
- raw event tape;
- secrets.

Known disruption information is identical.

Route and cost estimators are shared.

The LLM may use more reasoning steps, but it may not access more underlying information.

---

## 21. Route feasibility and execution

A candidate route is structurally feasible when:

- it begins at the shipment's current node;
- it ends at the shipment destination;
- it is a simple path;
- it contains at most six edges;
- every node and edge is currently available;
- every edge effective daily capacity is at least the shipment quantity;
- every departure node effective processing capacity is at least the shipment quantity.

A route may still wait because several shipments compete for the same current-day capacity.

Route estimation does not reserve capacity.

### 21.1 Current plan and `WAIT`

The current plan may include a temporarily unavailable component.

For `WAIT`, estimated arrival uses the earliest known reopening day and normal effective lead times after reopening.

If no known reopening exists, estimated arrival and cost are `None`; `WAIT` remains executable but is treated as infinitely costly by the heuristic unless no alternative exists.

### 21.2 Reroute

A `REROUTE` route:

- must be one of the observation's approved options;
- must contain no emergency edge;
- replaces the remaining route;
- incurs reroute cost.

### 21.3 Expedite

An `EXPEDITE` route:

- must be one of the observation's approved options;
- must contain at least one emergency edge;
- replaces the remaining route;
- incurs expedite premium;
- also incurs all normal edge transport costs.

### 21.4 Abstain

`ABSTAIN` is never directly executed in the simulator.

It invokes fallback.

---

## 22. Classical heuristic details

### 22.1 Candidate scoring

For every candidate action:

$$
\text{estimated\_total\_cost} = \text{remaining\_transport\_cost} + \text{action\_cost} + \text{estimated\_late\_penalty}
$$

The heuristic does not estimate future holding or backlog costs.

This limitation is intentional and makes the benchmark transparent.

### 22.2 Candidate eligibility

- `WAIT` is always eligible.
- `REROUTE` requires a non-emergency route.
- `EXPEDITE` requires an emergency route and:
  - `WAIT` lateness at least 2 days; or
  - no non-emergency candidate.

### 22.3 Choice

Select the eligible candidate with minimum estimated total cost.

Exact ties prefer:

```text
WAIT
REROUTE
EXPEDITE
```

If route candidates tie, choose the lexicographically smallest route ID after the shared route sort.

---

## 23. LLM-agent details

### 23.1 Decision key

Every LLM decision has:

```text
experiment_id
scenario_id
replication
run_kind
day
shipment_id
observation_hash
```

Canonical JSON uses sorted keys and compact separators.

`observation_hash` is SHA-256 of canonical observation JSON.

### 23.2 Live mode

In live mode:

- call the configured model;
- use approved tools;
- use strict schemas;
- set provider storage false;
- record the entire interaction except secrets;
- fail the experiment on provider failure after retries.

### 23.3 Replay mode

Replay mode must reproduce recorded actions without an API call.

The replay record must match every decision-key field.

Replay is the mechanism for exact reruns of an earlier LLM policy trace.

### 23.4 Model variability

Temperature zero reduces but does not guarantee deterministic provider behavior.

Therefore:

- the environment is seed-reproducible;
- live LLM interaction is fully recorded;
- exact policy replay uses recorded traces;
- claims of exact live-model reproducibility are forbidden.

### 23.5 Tool-call limit

The model may make at most eight tool calls per decision, including `submit_action`.

At the limit without submission, the policy returns `ABSTAIN`.

### 23.6 LLM prompt versioning

Define the prompt as a constant in `llm_agent.py`.

Calculate and write a SHA-256 prompt hash to the manifest.

Any prompt change is a policy change and must be mentioned in experiment comparison.

---

## 24. Capacity allocation

### 24.1 Edge capacity

Edge capacity counts quantity entering the edge that day.

It does not count shipments already in transit.

### 24.2 Node processing capacity

Node processing capacity counts quantity departing the node that day.

### 24.3 Allocation

For each ready shipment in deterministic order:

- check node remaining processing;
- check edge remaining capacity;
- dispatch only if both can hold the entire shipment;
- never partially dispatch;
- otherwise leave shipment at node.

The baseline shipment quantity is 40, ensuring the alternative and emergency routes can carry one complete shipment.

---

## 25. Drain phase and terminal handling

After day 60:

- demand is zero;
- no new shipments are released;
- ordinary delay tape remains available;
- policies continue making decisions;
- costs continue accruing;
- the simulator stops early when:
  - all shipments are delivered; and
  - backlog is zero.

Maximum drain ends on day 90.

If unresolved:

- apply terminal cost;
- record unresolved quantities;
- set `terminated_with_unresolved_state=true`;
- still return a result rather than silently discarding the replication.

---

## 26. Paired experiment design

For each replication $r$:

```text
one seed
one demand sequence
one edge-delay table
one release schedule
one warm-up snapshot
four cloned evaluation branches
```

Branches:

| Policy | World |
|---|---|
| Heuristic | Undisrupted |
| Heuristic | Disrupted |
| LLM | Undisrupted |
| LLM | Disrupted |

The undisrupted world removes only the designed shock.

Do not compare:

- a disrupted LLM run to a different demand seed;
- a policy-specific state produced by another branch;
- a live LLM run with a replay run without labeling it;
- different prompt versions in one aggregate summary.

---

## 27. Output directory and schemas

Output directory:

```text
outputs/<experiment_id>__<UTC_TIMESTAMP>/
```

Timestamp format:

```text
YYYYMMDDTHHMMSSZ
```

Generated files:

```text
manifest.json
resolved_config.yaml
run.log
event_tapes.jsonl
run_metrics.csv
daily_metrics.csv
decision_traces.jsonl
llm_interactions.jsonl
replications.csv
summary.json
```

### 27.1 `manifest.json`

Fields:

```text
experiment_id
created_at_utc
project_version
git_commit
git_dirty
python_version
platform
dependency_versions
network_config_sha256
scenario_config_sha256
heuristic_config_sha256
llm_config_sha256
experiment_config_sha256
llm_prompt_sha256
llm_execution_mode
llm_model
base_seed
replications
```

If Git is unavailable, use `null`.

### 27.2 `event_tapes.jsonl`

One line per replication:

```text
scenario_id
replication
seed
demand_events
shipment_release_events
edge_extra_delay_days
shocks
event_tape_sha256
```

### 27.3 `run_metrics.csv`

One row per branch.

Columns:

```text
experiment_id
scenario_id
replication
seed
policy
run_kind
total_cost
transport_cost
reroute_cost
expedite_cost
holding_cost
backlog_cost
late_cost
terminal_cost
same_day_fill_rate
final_fulfilment_rate
ending_backlog_units
backlog_unit_days
late_delivered_units
late_delivery_rate
average_lateness_days_weighted
reroute_count
expedite_count
expedited_units
decision_count
invalid_action_rate
abstention_rate
fallback_rate
mean_decision_latency_ms
days_to_clear_backlog_after_shock
terminated_with_unresolved_state
```

`run_kind` is `UNDISRUPTED` or `DISRUPTED`.

### 27.4 `daily_metrics.csv`

Columns:

```text
experiment_id
scenario_id
replication
policy
run_kind
day
inventory_units
backlog_units
shipments_at_node
shipments_in_transit
shipments_delivered
daily_demand_units
daily_same_day_fulfilled_units
daily_backlog_fulfilled_units
daily_transport_cost
daily_reroute_cost
daily_expedite_cost
daily_holding_cost
daily_backlog_cost
daily_late_cost
cumulative_total_cost
active_shock_ids
```

### 27.5 `decision_traces.jsonl`

One line per decision:

```text
decision_key
experiment_id
scenario_id
replication
policy
run_kind
day
shipment_id
observation_hash
observation
proposed_action
proposal_validation
fallback_invoked
fallback_action
fallback_validation
executed_action
decision_latency_ms
```

### 27.6 `llm_interactions.jsonl`

Only LLM decisions.

Fields:

```text
decision_key
model
prompt_hash
request_without_secrets
tool_calls
tool_outputs
provider_response
submitted_action
token_usage
latency_ms
attempt_count
```

### 27.7 `replications.csv`

One row per replication:

```text
replication
seed
heuristic_undisrupted_cost
heuristic_disrupted_cost
heuristic_tcd
llm_undisrupted_cost
llm_disrupted_cost
llm_tcd
delta
winner
```

### 27.8 `summary.json`

Contains:

- all experiment summary metrics;
- cost-component means by policy and world;
- service-metric means by policy and world;
- invalid, abstention, and fallback rates;
- unresolved-run counts;
- configuration and prompt hashes.

---

## 28. Logging and errors

Use the standard `logging` module.

Console:

- `INFO` progress;
- concise error message.

File:

- `DEBUG` detailed execution;
- no secrets;
- no hidden chain-of-thought.

Required exceptions:

```text
ConfigurationError
SimulationInvariantError
RoutingError
ActionValidationError
LLMIntegrationError
ReplayTraceError
OutputWriteError
```

Do not catch broad exceptions inside domain logic.

The CLI may catch at the top level to map exit codes and log tracebacks.

`fail_fast=true` means any branch failure stops the experiment.

---

## 29. State invariants checked every day

Assert:

- inventory is non-negative;
- backlog is non-negative;
- costs are non-negative;
- no shipment has negative quantity;
- every shipment status satisfies its field contract;
- delivered shipment count never decreases;
- an edge's used capacity does not exceed effective capacity;
- a node's used processing does not exceed effective capacity;
- no shipment enters an unavailable edge;
- no shipment departs an unavailable node;
- active shocks match the current day;
- base network values remain unchanged;
- route continuity is preserved;
- every shipment exists in exactly one physical state;
- total product balance is explainable by initial inventory, released quantity, fulfilled demand, inventory, backlog, and in-system shipments.

The balance assertion must account for backlog as unmet demand, not physical product.

---

## 30. Testing contract

## 30.1 `tests/unit/test_domain.py`

Must test the structural invariants of every domain enum and dataclass:

- Node, Edge, and Product field invariants (non-negative capacities, lead
  time at least 1, reliability in `[0, 1]`, distinct edge origin and
  destination, non-negative product costs);
- NetworkDefinition cross-reference integrity (duplicate keys, edges
  referencing unknown nodes) and its read-only lookup methods;
- Shipment status/field consistency for `AT_NODE`, `IN_TRANSIT`, and
  `DELIVERED`, and the positive-quantity invariant;
- DecisionAction route_id-by-action-type rules and the rationale length
  limit;
- ValidationResult's derived validity;
- DemandEvent, ShipmentReleaseEvent, and Shock field invariants.

## 30.2 `tests/unit/test_loaders.py`

Must test:

- the baseline and tiny network configs load into the exact expected typed
  objects;
- every malformed configuration this module is meant to reject actually
  fails loudly, with a precise error: extra fields, bad identifiers,
  duplicate identifiers, unknown cross-references, broken or misdirected
  route continuity, out-of-range values;
- path resolution rejects a missing referenced file and a path escaping the
  repository root;
- `build_network_definition` and `build_initial_state` produce the exact
  domain objects and day-0 state section 13.1 describes.

## 30.3 `tests/unit/test_event_tape.py`

Must test:

- stream-seed derivation is deterministic and distinct per stream name and
  per replication seed;
- demand generation is deterministic for a fixed seed and clamped to the
  configured minimum and maximum;
- edge-delay generation is deterministic for a fixed seed, in the correct
  draw order, and covers every edge on every day through the drain period;
- shipment-release events match the configured schedule exactly;
- a built event tape is reproducible for a fixed seed and replication, and
  differs across replications;
- the undisrupted tape differs from the disrupted tape only in `shocks` and
  `newly_known_shock_ids`; demand, releases, and delays stay identical.

## 30.4 `tests/unit/test_transition.py`

Must test:

- shipment release;
- edge entry and calculated arrival day;
- intermediate arrival;
- final delivery and inventory addition;
- node closure blocks arrival;
- capacity block increments waiting days;
- demand clears old backlog first;
- same-day demand fulfilment;
- backlog creation;
- daily event order;
- state invariants.

## 30.5 `tests/unit/test_routing.py`

Must test:

- directed route continuity;
- unavailable node removal;
- unavailable edge removal;
- emergency route identification;
- route sorting;
- route limit;
- cost estimate;
- lead-time estimate;
- known-reopening `WAIT` estimate;
- static-capacity filtering;
- deterministic output.

## 30.6 `tests/unit/test_costs.py`

Use manually calculated examples for:

- transport;
- reroute;
- expedite;
- holding;
- backlog;
- late delivery;
- terminal;
- total cost.

No approximate expected value may be generated by the same function under test.

## 30.7 `tests/unit/test_validator.py`

Test every validation code.

Also test:

- valid wait;
- valid reroute;
- valid expedite;
- abstain;
- route mismatch;
- emergency semantic mismatch;
- immutable action behavior.

## 30.8 `tests/unit/test_heuristic.py`

Test:

- wait is cheapest;
- reroute is cheapest;
- expedite is cheapest and eligible;
- expedite is cheapest but below trigger and therefore excluded;
- no alternative;
- exact tie preference;
- deterministic decision.

## 30.9 `tests/integration/test_full_simulation.py`

Run a tiny three-node network for a manually checkable horizon.

Assert exact:

- shipment positions;
- inventory;
- backlog;
- costs;
- final metrics;
- output-independent result determinism.

## 30.10 `tests/integration/test_paired_experiment.py`

Assert:

- all four branches start from deeply equal snapshots;
- branch state objects are distinct;
- disrupted and undisrupted tapes differ only by shocks;
- policies see equal observations for equal states;
- TCD and delta calculations are exact;
- output rows contain all four branches.

## 30.11 `tests/integration/test_reproducibility.py`

Assert:

- same config and seed produce identical event tape;
- heuristic runs are byte-for-byte metric identical;
- fake LLM runs are identical;
- replay mode reproduces recorded decisions;
- live LLM is excluded from deterministic CI tests.

## 30.12 Fixtures

`tiny_network.yaml`:

- one supplier;
- one hub;
- one plant;
- one normal route;
- one emergency route;
- deterministic demand;
- reliability `1.0`.

`tiny_scenario.yaml`:

- one short edge closure.

`expected_results.json`:

- manually calculated states and costs.

## 30.13 Test isolation

Tests must:

- never use a real API;
- never depend on wall-clock time;
- use temporary output directories;
- avoid global mutable state;
- use fixed seeds;
- complete quickly.

---

## 31. Implementation methodology

Every implementation task follows:

### Step 1 — Inspect

Read:

- this file;
- relevant source;
- relevant tests;
- current Git diff.

### Step 2 — State task contract

Before edits, state:

```text
Goal:
Allowed files:
Behavior that must remain unchanged:
Done when:
Verification:
Concerns:
```

### Step 3 — Implement smallest vertical unit

Prefer one behavior with its test over many unverified files.

### Step 4 — Verify locally

Run targeted tests first.

### Step 5 — Verify shared behavior

Run broader tests when shared modules change.

### Step 6 — Inspect final diff

Confirm:

- no unrelated files;
- no accidental formatting;
- no stale comments;
- no dead code;
- docstrings are accurate;
- configuration and tests agree.

### Step 7 — Report evidence

Report exact commands and outcomes.

---

## 32. Build sequence and milestone contracts

## Milestone 0 — Repository bootstrap

Implement:

- root files;
- package folders;
- empty `__init__.py` files with proper docstrings;
- dependencies and tooling;
- baseline configs;
- configuration validation skeleton.

Done when:

- package imports;
- configs parse;
- Ruff, Mypy, and Pytest run;
- no simulation behavior exists yet.

## Milestone 1 — Domain model and configuration

Implement:

- all enums and dataclasses;
- Pydantic configuration classes;
- loaders;
- network and route validation;
- day-0 state construction.

Done when:

- baseline and tiny configs load into exact domain objects;
- invalid fixtures fail with precise errors;
- all domain invariants have unit tests.

## Milestone 2 — Event tape

Implement:

- seed derivation;
- demand;
- delay table;
- shipment releases;
- shocks;
- disrupted/undisrupted pairing;
- event-tape serialization.

Done when:

- fixed seeds reproduce exact tapes;
- removing the scenario leaves all ordinary events unchanged.

## Milestone 3 — Routing and cost model

Implement:

- operational graph;
- candidate routes;
- estimates;
- cost functions.

Done when:

- route options are deterministic;
- manual cost tests pass;
- emergency semantics are correct.

## Milestone 4 — Core simulation without policies

Implement:

- day loop;
- releases;
- arrivals;
- demand;
- backlog;
- automatic movement;
- capacity;
- warm-up;
- drain;
- daily metrics.

Use decisions disabled or `WAIT`.

Done when:

- tiny full simulation matches manually calculated expected results.

## Milestone 5 — Observation, actions, and validation

Implement:

- observations;
- triggers;
- action schemas;
- validator;
- safe wait.

Done when:

- all policies can receive an immutable observation;
- every validation code is tested;
- state cannot be changed through an observation.

## Milestone 6 — Heuristic policy

Implement exact heuristic and fallback.

Done when:

- all heuristic decision tests pass;
- one disrupted heuristic simulation completes;
- every proposed and executed action is traceable.

## Milestone 7 — Experiment runner and outputs

Implement:

- warm-up snapshot;
- four branches;
- TCD;
- delta;
- metrics;
- output writer;
- manifest;
- CLI.

Done when:

- a multi-replication heuristic-versus-fake-policy experiment writes all required files;
- paired invariants are tested.

## Milestone 8 — LLM integration

Implement:

- OpenAI client;
- tools;
- agent loop;
- strict submission;
- retries;
- audit logging;
- replay mode;
- fake client.

Done when:

- fake and replay integration tests pass;
- one opt-in live smoke test can complete outside automated tests;
- no secret appears in logs.

## Milestone 9 — Baseline end-to-end experiment

Run:

- config validation;
- unit tests;
- integration tests;
- lint;
- type checks;
- a small two-replication live experiment;
- then the configured 100-replication experiment when API budget permits.

Done when:

- all acceptance criteria below hold;
- outputs are complete;
- results can be reconstructed from saved configuration, event tapes, and LLM traces.

Do not start a milestone whose required predecessor is failing.

---

## 33. Version 1 acceptance criteria

Version 1 is complete only when all are true:

### Architecture

- exact source tree exists;
- no unapproved architectural folder exists;
- dependency direction is respected;
- every human-authored file has an accurate opening explanation.

### Simulation

- baseline network loads;
- daily state transition is deterministic;
- warm-up produces a stable snapshot;
- shipments move correctly;
- demand, inventory, backlog, capacity, and costs work;
- port closure semantics match this contract;
- drain and terminal handling work.

### Fairness

- all compared branches use deep clones;
- event tapes are paired;
- undisrupted tapes remove only shocks;
- observations are symmetric;
- routing, validation, and costs are shared;
- no LLM tool modifies state;
- every action is validated.

### Policies

- heuristic follows exact rule;
- LLM uses only approved tools;
- invalid and abstaining LLM outputs invoke fallback;
- fallback is logged;
- live failures stop the experiment;
- replay reproduces recorded actions.

### Metrics

- all run metrics are calculated;
- TCD is policy-specific;
- delta is paired;
- confidence interval and winner rates are reported;
- no failed replication is silently omitted.

### Reproducibility and audit

- seeds and event tapes are written;
- resolved configs are written;
- config and prompt hashes are written;
- decisions are written;
- LLM interactions are written;
- heuristic and replay runs are reproducible.

### Quality

- all tests pass;
- Ruff passes;
- Mypy passes;
- coverage is at least 90%;
- no real API call occurs in tests;
- no secret exists in repository or outputs;
- README run instructions work from a clean environment.

---

## 34. Definition of done for any coding task

A task is done only when:

- the requested behavior is implemented;
- the implementation follows this contract;
- the smallest coherent diff was used;
- applicable tests were added or updated;
- targeted tests pass;
- shared tests pass when required;
- Ruff and Mypy pass for touched code;
- generated or modified files have accurate opening documentation;
- no unrelated behavior changed;
- no debug code remains;
- assumptions and unverified items are disclosed;
- exact verification evidence is reported.

“Looks correct,” “should work,” and “implemented” without verification do not satisfy done.

---

## 35. Prohibited implementation shortcuts

Do not:

- place all models in one untyped dictionary;
- use mutable default arguments;
- hide random draws inside transition functions;
- query the live LLM from simulation physics;
- let policies edit shipments;
- calculate different route options per policy;
- silently skip invalid configuration;
- silently coerce strings to numerical values;
- partially dispatch shipments;
- repair invented route IDs;
- use current wall time as a random seed;
- use separate random streams per policy;
- rely on dictionary order;
- catch and ignore exceptions;
- continue after a failed branch;
- add a database;
- add a UI;
- add concurrency;
- add multi-product behavior;
- add hidden environment-variable overrides;
- modify this contract without approval.

---

## 36. Expected first successful baseline execution

A correct first full run will:

1. load the four configuration files;
2. validate the baseline network;
3. create output directory and manifest;
4. generate replication 1 event tape;
5. warm up through day 20;
6. fork four branches;
7. activate the primary-port closure in disrupted branches on day 21;
8. let both policies make shipment-level decisions;
9. execute only validated actions;
10. continue through day 60 and drain if needed;
11. calculate heuristic and LLM disrupted/undisrupted costs;
12. calculate TCD and delta;
13. repeat for every replication;
14. save complete traces;
15. write aggregate summary.

A plausible result may favor either policy. The project is not successful because the LLM wins; it is successful because the comparison is fair, executable, auditable, and scientifically interpretable.

---

## 37. Final locked decisions for Version 1

The following are settled:

| Topic | Version 1 decision |
|---|---|
| Time resolution | Daily |
| Product count | One |
| Decision unit | Complete shipment |
| Shipment splitting | No |
| Policy action timing | Shipment at node only |
| Action space | WAIT, REROUTE, EXPEDITE, ABSTAIN |
| Baseline disruption | Primary-port closure, days 21–27 |
| Shock information | Complete and immediate |
| Demand | Truncated normal, pre-generated |
| Ordinary delays | Pre-generated 0/1 edge-day delay |
| Randomness | Shared paired event tape |
| Warm-up | 20 days, decisions disabled |
| Evaluation horizon | Day 60 |
| Drain | Up to 30 days |
| Counterfactual | Policy-specific |
| Heuristic | Lowest estimated candidate cost |
| LLM tools | Five approved read-only/submission tools |
| LLM fallback | Heuristic, then terminal WAIT |
| Live provider failure | Fail experiment |
| Exact LLM rerun | Replay recorded trace |
| Output storage | Files only |
| Runtime architecture | Modular monolith |
| CLI | argparse, validate-config and run |
| Tests | No live API |
| Primary metric | Paired TCD delta |
| Success condition | Valid fair comparison, not an LLM win |

No coding agent may change these decisions without explicit approval from Yassine.

---

# Part 2 — Version 2 Build Contract

## Why V2 exists

V1 answered its research question cleanly: across 300 real replications (100 each for a Light/Medium/Heavy disruption), the LLM agent beat the classical heuristic overwhelmingly under Medium and Heavy disruptions and lost consistently under Light ones. But auditing those results surfaced something the V1 design didn't anticipate: **within every single profile, all 100 replications agreed on the winner — 100%/0%, never a mixed result.** A full audit (see `REPORT.md` and the conversation history around it) traced this to three compounding, fixable properties of V1's design, not to a flaw in the comparison itself:

1. The mean cost gap between policies is 7.6×–10.2× larger than the random noise between replications, in every profile — the two policies are separated by a gap the environment's randomness was never designed to be able to bridge.
2. The shipment release schedule is **fully deterministic** (exactly 40 units, every single day) and the disruption's timing, duration, and disclosure are all **fully known in advance** — so the same handful of shipments hit the same disruption the same way in every replication, regardless of the random demand/delay draws.
3. The network has only 5 nodes, 6 edges, and effectively 3 end-to-end routes — not enough structural diversity for decisions to ever land close to a genuine tie.

V2 exists to fix exactly these three things — and only these three, plus the richer disruption taxonomy that falls naturally out of fixing them — without touching anything about V1 that already works. **V1's simulation physics, cost model, action space, policy protocol, LLM integration, CLI, and output schemas are not being redesigned.** V2 extends the *inputs* those mechanisms already handle correctly: more varied topologies, more varied and realistic disruptions, and more sources of randomness feeding the same paired-replication machinery V1 already validated.

## V2.1 Mission delta

V1's research question was:

> Under identical disrupted supply-chain conditions and operational constraints, can a bounded LLM-enabled policy produce valid shipment-level decisions that reduce the incremental cost of disruption more effectively and robustly than a classical heuristic?

V2 extends it to explicitly make topology and disruption character first-class variables, not fixed backdrop:

> **Across a range of network topologies and a realistic range of disruption types, timings, and severities — each with genuine, unresolved-until-it-happens uncertainty — does a bounded LLM-enabled policy produce valid shipment-level decisions that reduce the incremental cost of disruption more effectively and more robustly than a classical heuristic, and how does that advantage vary with network complexity and disruption character?**

The system must still compare policies, not give one a more favorable world — that invariant from V1 §2 is unchanged and, if anything, more load-bearing now that topology and disruption realization both vary: every new source of variation introduced below must still be drawn once, before either policy sees it, and shared identically between the heuristic and LLM branches for a given replication and topology cell.

## V2.2 Scope changes from Version 1

### V1 non-goals now explicitly in scope

Two items V1 §3.3 explicitly excluded are now, deliberately, in scope:

- **"Uncertain disruption duration"** — a disruption's realized duration is now drawn from a distribution per replication, not fixed in the scenario file.
- **"Delayed or asymmetric information"** — a disruption's `information_day` may now fall after its `physical_start_day`, drawn from a distribution, so policies can genuinely be caught by surprise.

Everything else in V1 §3.3's non-goals list **stays excluded** — V2 does not introduce multi-product handling, supplier-selection decisions, procurement, shipment splitting, joint multi-shipment optimization, dynamic pricing, fleet-level routing, reinforcement learning, a database, a web UI, or distributed/real-time execution. Supplier-side shocks (V2.3.2) make a supplier node disruptable; they do not make supplier *choice* a policy action — the policy still only ever chooses among WAIT / REROUTE / EXPEDITE / ABSTAIN for an existing shipment, exactly as in V1 §3.2.

### New in scope

- **Network topology as an experimental variable.** Three topology tiers (V2.3.1), crossed with the three severity tiers V1 already built, forming a 3×3 experimental grid (V2.8).
- **A richer shock taxonomy**: demand-side shocks, supplier-side shocks, and compound/cascading multi-shock events (V2.3.2–V2.3.4), alongside V1's existing six network-side shock types, which are unchanged.
- **Shock realization from distributions** rather than fixed scenario values (V2.3.3): start-day jitter, duration uncertainty, and information delay are all sampled once per replication from a documented distribution, using the same deterministic, auditable seeding approach V1 already uses for demand and ordinary delays.
- **Shipment quantity randomness** in the release schedule (V2.3.5): release timing stays on V1's fixed daily cadence (this is a deliberate, scoped choice — see V2.3.5), but the quantity released each day is now drawn from a distribution instead of always being exactly 40 units.

### What must not change

- The four-action policy interface (WAIT / REROUTE / EXPEDITE / ABSTAIN), the shared validator, the fallback chain, and the LLM's five approved tools — all unchanged.
- The daily event order (V1 §14) — unchanged in sequence; V2.3.7 lists the two narrow, additive exceptions required to keep it correct under the new randomness.
- The cost model (V1 §11.7, §5.7) and every cost formula in V1 §11.7 — unchanged. New shock types produce new *inputs* to existing formulas (e.g., a demand shock changes `D_t`; it does not add a new cost term).
- The paired TCD/delta metric (V1 §5.8) — unchanged in definition. It is now computed per (topology tier, severity tier) cell instead of once globally; V2.8 defines exactly how the aggregate reporting extends.
- Every already-built package (`domain`, `simulation`, `decisions`, `policies`, `experiments`, `integrations`, `data_io`) keeps its existing responsibility boundaries (V1 §10). V2's new mechanics are additive to these packages, not a new architectural layer.

## V2.3 Formal model extensions

### V2.3.1 Topology tiers

Three fixed network topologies, independent of and crossed with the three severity tiers (V2.8). **Naming is deliberately distinct from the severity tiers** ("Light/Medium/Heavy") to prevent the two axes from ever being confused in code, config, output files, or conversation: topology tiers are named **Compact / Standard / Extended**.

- **Standard** is V1's existing, unmodified `configs/networks/baseline_network.yaml` (5 nodes, 6 edges) — kept byte-identical so every V1 result remains a valid, comparable reference point inside the V2 grid (it is exactly the "Standard × Medium" cell).
- **Compact** removes the alternate port entirely, so a primary-port disruption leaves REROUTE structurally impossible — only WAIT and EXPEDITE remain, a deliberately more constrained decision environment than Standard.
- **Extended** adds a third port and a second hub, giving genuine mesh redundancy — two viable multi-hop alternatives to the primary route instead of Standard's one.

All three tiers share the exact same product, initial inventory, demand process, and (template) replenishment plan (V2.3.5) — **only the network's nodes and edges differ between tiers.** This isolates topology's effect on routing flexibility from any confound with shipment volume or demand.

#### Compact (`configs/networks/topology_compact.yaml`)

**Nodes (4):**

| node_id | node_type | storage_capacity | processing_capacity | source_capacity |
|---|---|---:|---:|---:|
| `supplier_1` | SUPPLIER | 2000 | 200 | 100 |
| `port_primary` | PORT | 2000 | 200 | 0 |
| `hub_1` | HUB | 2000 | 200 | 0 |
| `plant_1` | PLANT | 1000 | 200 | 0 |

**Edges (4):**

| edge_id | origin → destination | mode | distance_km | base_lead_time_days | daily_capacity | unit_transport_cost | reliability | emergency |
|---|---|---|---:|---:|---:|---:|---:|---|
| `supplier_to_primary_port` | supplier_1 → port_primary | ROAD | 400 | 2 | 100 | 4.00 | 0.97 | false |
| `primary_port_to_hub` | port_primary → hub_1 | SEA | 6000 | 10 | 200 | 8.00 | 0.94 | false |
| `hub_to_plant` | hub_1 → plant_1 | ROAD | 300 | 2 | 120 | 3.00 | 0.98 | false |
| `supplier_to_plant_air` | supplier_1 → plant_1 | AIR | 6200 | 2 | 40 | 40.00 | 0.99 | **true** |

Every value is copied unchanged from Standard's corresponding edge — Compact is Standard minus the alternate port and its two edges, nothing more, so any behavioral difference between Compact and Standard is attributable to route *availability*, not to different underlying per-edge economics.

#### Standard (`configs/networks/baseline_network.yaml`)

Unchanged from V1 §12.1 — 5 nodes, 6 edges. Not reproduced here; see V1 §12.1 for the exact file content.

#### Extended (`configs/networks/topology_extended.yaml`)

**Nodes (7):**

| node_id | node_type | storage_capacity | processing_capacity | source_capacity |
|---|---|---:|---:|---:|
| `supplier_1` | SUPPLIER | 2000 | 200 | 100 |
| `port_primary` | PORT | 2000 | 200 | 0 |
| `port_alternative` | PORT | 1200 | 80 | 0 |
| `port_tertiary` | PORT | 1200 | 90 | 0 |
| `hub_1` | HUB | 2000 | 200 | 0 |
| `hub_2` | HUB | 1500 | 150 | 0 |
| `plant_1` | PLANT | 1000 | 200 | 0 |

`supplier_1` and `plant_1` are deliberately identical to Standard/Compact (same source and demand-side economics across every tier — richness is added only in the middle of the network).

**Edges (10):**

| edge_id | origin → destination | mode | distance_km | base_lead_time_days | daily_capacity | unit_transport_cost | reliability | emergency |
|---|---|---|---:|---:|---:|---:|---:|---|
| `supplier_to_primary_port` | supplier_1 → port_primary | ROAD | 400 | 2 | 100 | 4.00 | 0.97 | false |
| `supplier_to_alternative_port` | supplier_1 → port_alternative | ROAD | 650 | 4 | 60 | 7.00 | 0.96 | false |
| `supplier_to_tertiary_port` | supplier_1 → port_tertiary | ROAD | 800 | 5 | 60 | 8.50 | 0.95 | false |
| `primary_port_to_hub_1` | port_primary → hub_1 | SEA | 6000 | 10 | 200 | 8.00 | 0.94 | false |
| `alternative_port_to_hub_1` | port_alternative → hub_1 | SEA | 6700 | 12 | 60 | 11.00 | 0.93 | false |
| `alternative_port_to_hub_2` | port_alternative → hub_2 | SEA | 5800 | 9 | 50 | 10.00 | 0.94 | false |
| `tertiary_port_to_hub_2` | port_tertiary → hub_2 | SEA | 6200 | 11 | 55 | 10.50 | 0.93 | false |
| `hub_1_to_plant` | hub_1 → plant_1 | ROAD | 300 | 2 | 120 | 3.00 | 0.98 | false |
| `hub_2_to_plant` | hub_2 → plant_1 | ROAD | 350 | 3 | 100 | 3.50 | 0.97 | false |
| `supplier_to_plant_air` | supplier_1 → plant_1 | AIR | 6200 | 2 | 40 | 40.00 | 0.99 | **true** |

Every edge shared with Standard (`supplier_to_primary_port`, `supplier_to_alternative_port`, the primary/alternative-to-hub legs, `supplier_to_plant_air`) keeps Standard's exact values, renamed only where a second hub required disambiguating the edge id (`primary_port_to_hub_1`, `alternative_port_to_hub_1`, `hub_1_to_plant` are Standard's `primary_port_to_hub`, `alternative_port_to_hub`, `hub_to_plant` respectively, under new ids since Extended has more than one hub). `port_alternative` now reaches **both** hubs, giving two genuinely distinct multi-hop alternatives to the primary route (`port_primary → hub_1` vs. `port_tertiary → hub_2`), plus a third partial alternative through the shared `port_alternative` node.

**Default route:** in every tier, the replenishment plan's `initial_route_edge_ids` is the "primary" path — `supplier_to_primary_port → primary_port_to_hub(_1) → hub_to_plant` (`hub_1_to_plant` in Extended). The additional infrastructure in Standard and Extended only matters once that primary path is disrupted and candidate-route enumeration (V1 §11.6) considers alternatives — which is exactly the mechanism V1 already built and does not change.

### V2.3.2 Extended shock taxonomy

V1's six shock types (`domain/events.py`'s `ShockType` enum) are unchanged and keep working exactly as built. V2 adds three new shock types and one new target kind:

```text
ShockType additions:
    DEMAND_SPIKE
    DEMAND_DROP
    SUPPLIER_CAPACITY_REDUCTION

TargetType addition:
    DEMAND
```

("Supplier closure" needs no new shock type — it is `NODE_CLOSURE` targeting a `SUPPLIER`-type node, which V1's enum already supports. What changes is that `release_shipments` must now handle a closed or under-capacity supplier gracefully instead of raising an invariant error — see V2.3.7.)

**`DEMAND_SPIKE` / `DEMAND_DROP`** (`target_type: DEMAND`, `target_id` = the demand process's `destination_node_id`, e.g. `plant_1`): while active, multiplies the demand process's mean and its clamp bounds by a new `demand_multiplier` field (`NonNegativeFloat`, default `1.0`) — `DEMAND_SPIKE` scenarios use a multiplier `> 1.0`, `DEMAND_DROP` scenarios use one `< 1.0`. This is a **generation-time** effect, not a runtime one (V2.3.6): demand events are pre-generated once per replication exactly as in V1, so a demand shock's effect is baked into the generated event tape when it is built, not re-evaluated day by day during simulation. No new field is reused for this — `capacity_multiplier` already has an established meaning (departure/processing throughput) and reusing it for demand would be a silent, confusing overload, so `demand_multiplier` is deliberately a new, separate field.

**`SUPPLIER_CAPACITY_REDUCTION`** (`target_type: NODE`, `target_id` must be a `SUPPLIER`-type node): reduces the supplier's *release* capacity, using the existing `capacity_multiplier` field (this reuse is deliberate and safe: V1's `NODE_CAPACITY_REDUCTION` only ever touches `processing_capacity_multiplier`, a different operational dimension, so there is no collision). This requires one new field on `OperationalNodeState` (`source_capacity_multiplier: float = 1.0`, V2.4) and one new branch in `apply_shock_operational_state` — otherwise identical in shape to how `NODE_CAPACITY_REDUCTION` already works.

### V2.3.3 Shock realization: templates and distributions

This is the mechanism that answers the audit's core finding: in V1, a shock's start day, end day, and information day are fixed values written directly into the scenario YAML, identical in every replication. In V2, a scenario defines **shock templates** — the same fields as V1's `Shock`, plus distribution parameters — and one concrete `Shock` per template is **sampled once per replication**, before either policy makes a single decision, using the same deterministic, auditable seeding V1 already uses for demand and ordinary delays (V1 §17, §23.4).

**New template fields** (replacing V1's fixed `physical_start_day` / `physical_end_day` / `information_day` in the scenario schema — the realized `Shock` domain object keeps those exact three fields unchanged, per V2.3.6):

| Field | Type | Meaning |
|---|---:|---|
| `planned_start_day` | `int` | Nominal/anchor start day — same role V1's `physical_start_day` played |
| `start_day_jitter_days` | `NonNegativeInt`, default `0` | Realized start day = `planned_start_day + Uniform{-J, ..., +J}` (discrete, inclusive). `0` reproduces V1's fixed-start-day behavior exactly. |
| `minimum_duration_days` | `PositiveInt` | Hard floor on realized duration |
| `duration_mean_days` | `PositiveFloat` | Mean of the duration distribution |
| `duration_std_days` | `NonNegativeFloat`, default `0` | Std. dev. of the duration distribution. `0` reproduces V1's fixed-duration behavior exactly. |
| `maximum_duration_days` | `PositiveInt` | Hard ceiling on realized duration |
| `max_information_delay_days` | `NonNegativeInt`, default `0` | Realized delay = discrete `Uniform{0, ..., max_information_delay_days}`. `0` reproduces V1's "complete and immediate" information behavior exactly. |

**Realization formulas**, computed once per replication per template, in this fixed order (matching V1 §17's "draw order within a stream is deterministic" convention):

1. `start_day_jitter = Uniform{-start_day_jitter_days, ..., +start_day_jitter_days}` → `physical_start_day = planned_start_day + start_day_jitter`
2. `duration_days = round(TruncatedNormal(duration_mean_days, duration_std_days, min=minimum_duration_days, max=maximum_duration_days))` (identical distribution family and clamping convention to V1's demand generation, V1 §17) → `physical_end_day = physical_start_day + duration_days - 1`
3. `information_delay = Uniform{0, ..., max_information_delay_days}` → `information_day = min(physical_start_day + information_delay, physical_end_day)` — clamped so information can never arrive *after* the disruption it describes has already ended; a policy may still learn about a disruption on the day it ends, but never learn about it only in retrospect.

The three draws for one template are always made in that order (start day, then duration, then information delay), and templates within one scenario are realized in ascending `shock_id` order — both fixed, so a given `(base_seed, replication)` pair always reproduces the identical realized shocks, exactly as V1's existing reproducibility guarantee (V1 §23.4, V1 §30.3) requires.

**Where this happens, and why the simulation engine does not change:** realization happens entirely inside `experiments/event_tape.py`, before the event tape is handed to `simulation/engine.py`. The output of realization is a tuple of ordinary `domain.events.Shock` objects — the exact same frozen dataclass V1 already has, with concrete `physical_start_day` / `physical_end_day` / `information_day` values. `simulation/transition.py`'s `apply_shock_operational_state` and every other consumer of `Shock` objects need **zero changes** for node/edge shock types: they already only ever look at the realized, concrete values. Only `DEMAND_SPIKE`/`DEMAND_DROP` need a second, narrow change to `generate_demand_events` (V2.3.6), since demand is pre-generated rather than evaluated at runtime.

**A degenerate window is a configuration error, not a runtime concern:** if `start_day_jitter_days` and `duration_std_days` are both `0` and `max_information_delay_days` is `0`, realization always reproduces the exact fixed values a V1-style scenario would have used. V1's existing `port_closure.yaml`, `port_partial_capacity.yaml`, and `port_extended_closure.yaml` remain valid V2 templates unchanged, simply with all-zero uncertainty fields — nothing forces existing V1 scenarios to be rewritten.

### V2.3.4 Compound and cascading disruption events

A scenario may group multiple shock templates under a shared `event_group_id` (`str | None`, default `None`). Templates sharing a group id represent effects of **one underlying event** (a regional storm, a geopolitical closure, a single supplier's plant fire cascading into a capacity shortfall) and share **one** realized `start_day_jitter` draw — all members start their jitter from the same random offset — while each member still draws its **own** `duration_days` and `information_delay` independently, representing that correlated events start together but each affected asset recovers, and is disclosed, on its own timeline. Templates with `event_group_id: null` are realized fully independently, exactly as V2.3.3 describes for a single template — this is the default, so ungrouped scenarios (including all of V1's existing ones, ported forward) need no changes.

Realization order for a scenario with groups: process `event_group_id`s in ascending lexicographic order (treating `null` as sorting last, as one group per ungrouped template); for a real group, draw the one shared `start_day_jitter` first, then realize each member template's `duration_days` and `information_delay` in ascending `shock_id` order within the group. This keeps the whole scenario's realization fully deterministic and auditable, consistent with V2.3.3's single-template ordering rule.

### V2.3.5 Release schedule realization

V1's replenishment plan releases exactly `shipment_quantity` units every `release_every_days` days, with zero variation, ever — the audit identified this as the single largest contributor to the 0%/100% win rates, since the set of shipments any disruption catches is otherwise fully predictable. V2 makes the **quantity** stochastic; it deliberately leaves the **timing** fixed.

**Why timing stays fixed and quantity does not:** randomizing release timing would ripple into due-date computation, shipment-id generation (V1 §6.4's `shipment_<release_day>_<sequence>` scheme assumes one release per calendar day), and the deterministic-ordering rules used throughout decision-triggering and departure allocation (V1 §6.5) — a much larger, riskier change for a benefit ("which day" varies) that timing jitter on the *disruption* (V2.3.3) already delivers more directly and more safely. Quantity variation is additive, narrow, and touches only the release-event generation formula and one new feasibility clamp.

**New replenishment plan template fields** (config-level, alongside V1's existing `shipment_quantity`, which becomes the distribution's mean):

| Field | Type | Meaning |
|---|---:|---|
| `shipment_quantity_mean` | `PositiveFloat` | Replaces V1's fixed `shipment_quantity` as the distribution's mean |
| `shipment_quantity_std` | `NonNegativeFloat`, default `0` | Std. dev. `0` reproduces V1's fixed-quantity behavior exactly |
| `minimum_shipment_quantity` | `PositiveInt` | Hard floor |
| `maximum_shipment_quantity` | `PositiveInt` | Hard ceiling |

**Realized quantity**, drawn once per scheduled release event, in release-day order: `quantity = round(TruncatedNormal(shipment_quantity_mean, shipment_quantity_std, min=minimum_shipment_quantity, max=maximum_shipment_quantity))` — identical distribution family and clamping convention to V1's demand generation (V1 §17) and V2.3.3's duration realization, for consistency.

**Feasibility clamp:** V1 §6.2 requires "every shipment quantity must fit the static capacity of every edge in its configured initial route" — a config-load-time guarantee in V1, since quantity was fixed. With quantity now random, this becomes a **generation-time** clamp instead: after drawing a raw quantity, it is further clamped to `min(raw_quantity, minimum static capacity across every edge and every node's processing_capacity on the initial route)`, deterministically, before the release event is finalized. This guarantees every generated `ShipmentReleaseEvent` remains structurally feasible on its initial route, exactly preserving V1's existing invariant, without requiring a config-time check that quantity can no longer statically satisfy.

### V2.3.6 Updated randomness streams

V1 §17 requires exactly two deterministic streams per replication: `demand` and `edge_delays`. V2 adds two more, derived with the identical mechanism (`sha256(f"{replication_seed}:{stream_name}")`, first eight bytes as an unsigned integer, per V1 §17):

```text
Required streams (V2):
    demand              (V1, unchanged)
    edge_delays         (V1, unchanged)
    shock_realization   (V2.3.3, V2.3.4 -- start-day jitter, duration, information delay)
    release_quantity    (V2.3.5 -- shipment quantity)
```

Draw order within each new stream follows the same style of rule V1 already uses for its two streams: `shock_realization` draws proceed in the scenario's fixed template/group order (V2.3.3–V2.3.4); `release_quantity` draws proceed in ascending scheduled-release-day order. As in V1, every draw happens once, up front, before either policy makes a single decision — nothing about V2's new randomness is drawn lazily or on demand during the day loop.

**Demand shocks and the demand stream:** `DEMAND_SPIKE`/`DEMAND_DROP` do not add a new draw to the `demand` stream — they change the *parameters* (`generate_demand_events` reads the realized shocks and, for any day inside an active demand shock's realized window, uses `mean_daily_demand * demand_multiplier` and equivalently scaled clamp bounds in place of the base values) but the random number consumed from the `demand` stream's `rng.gauss(...)` call is drawn exactly once per day exactly as in V1 — only what it is drawn *around* changes. This keeps the `demand` stream's draw count and draw order byte-identical to V1 regardless of whether a demand shock is present, which matters for V2.3.7's undisrupted-counterfactual rule below.

**Undisrupted counterfactual, extended:** V1's undisrupted tape is built by stripping only the scenario's shocks from an otherwise-identical disrupted tape (V1 §17, "the undisrupted tape is a copy with the designed shocks removed"). This is unchanged in V2 and now also applies uniformly across every new shock type: removing a realized `DEMAND_SPIKE`/`DEMAND_DROP`/`SUPPLIER_CAPACITY_REDUCTION`/compound-group shock from the disrupted tape and regenerating demand for that tape's days without it produces the fair "what if this hadn't happened" counterfactual, exactly as removing a `NODE_CLOSURE` already does today.

### V2.3.7 Daily event order: the one required change

V1 §14's thirteen-step daily order (reveal information → apply shocks → process arrivals → release shipments → fulfil demand → build/resolve decisions → allocate departures → charge costs → record metrics) is **unchanged in sequence.** Demand-shock effects are already fully baked into step 6 by generation time (V2.3.6), so step 6 itself needs no runtime awareness of shocks, exactly as before. Supplier capacity shocks are handled by step 3 (`apply_shock_operational_state` gains one new branch, V2.3.2) exactly as every other node/edge shock already is.

The one required change is to **step 5, shipment release**, and it exists specifically to make `SUPPLIER_CLOSURE` (`NODE_CLOSURE` targeting a supplier) and `SUPPLIER_CAPACITY_REDUCTION` usable at all: V1's `release_shipments` raises `SimulationInvariantError` if a scheduled release's origin is unavailable, over capacity, or would overflow storage, because V1's baseline configuration is designed to guarantee this never happens (V1 §14 step 5). V2 introduces supplier-side shocks specifically to make it happen on purpose, so this can no longer be treated as an invariant violation.

**New behavior:** `SimulationState` gains one new field, `pending_releases: list[ShipmentReleaseEvent]` (default empty). Step 5 becomes:

1. Attempt every event currently in `pending_releases`, in ascending `shipment_id` order (i.e., oldest-scheduled first — a first-committed-first-served rule), removing each from `pending_releases` on success.
2. Attempt every event newly scheduled for today, in the existing V1 §6.5 order (ascending `shipment_id`).
3. For either group, if the origin node is unavailable, over its effective `source_capacity` (V2.3.2's `source_capacity_multiplier`, floored exactly as every other effective-capacity calculation in V1 §5.4), or would overflow storage: **append the event to `pending_releases` and continue** — this is no longer a `SimulationInvariantError`.

`due_day` is unaffected by deferral: it was already computed once, at event-tape generation time, from the shipment's *originally scheduled* release day (V1's existing `ShipmentReleaseEvent.due_day = day + due_offset_days`), not from whenever `release_shipments` actually succeeds — so a supplier closure that delays a release automatically and correctly produces a later, costlier delivery relative to a due date the shipment was always going to be held to. No change to due-date computation is needed; this behavior already falls out of V1's existing design.

If a pending release still hasn't succeeded by the final drain day, it simply never entered the system — `total_released` (V1's product-balance invariant, V1 §29) only ever counts shipments that actually released, so the balance assertion remains correct without modification.

## V2.4 Domain model deltas

Every V2 mechanic above reduces to four small, additive changes to already-built dataclasses — nothing existing is renamed, removed, or given new required fields that would break a V1 caller:

```python
# domain/state.py
@dataclass(slots=True)
class OperationalNodeState:
    available: bool = True
    processing_capacity_multiplier: float = 1.0
    source_capacity_multiplier: float = 1.0          # NEW (V2.3.2)

@dataclass(slots=True)
class SimulationState:
    ...                                                 # every V1 field, unchanged
    pending_releases: list[ShipmentReleaseEvent] = field(default_factory=list)  # NEW (V2.3.7)
```

```python
# domain/events.py
class ShockType(Enum):
    ...                                                 # every V1 value, unchanged
    DEMAND_SPIKE = "DEMAND_SPIKE"                        # NEW (V2.3.2)
    DEMAND_DROP = "DEMAND_DROP"                          # NEW (V2.3.2)
    SUPPLIER_CAPACITY_REDUCTION = "SUPPLIER_CAPACITY_REDUCTION"  # NEW (V2.3.2)

class TargetType(Enum):
    NODE = "NODE"                                        # V1, unchanged
    EDGE = "EDGE"                                         # V1, unchanged
    DEMAND = "DEMAND"                                     # NEW (V2.3.2)

@dataclass(frozen=True, slots=True)
class Shock:
    ...                                                 # every V1 field, unchanged
    demand_multiplier: float = 1.0                       # NEW (V2.3.2); unused (stays 1.0) for every shock type except DEMAND_SPIKE/DEMAND_DROP, per V1 §18.2's existing "unused multiplier fields remain 1.0" convention
```

No change to `Node`, `Edge`, `Product`, `Route`, `DecisionAction`, `ValidationResult`, `ShipmentStatus`, `CostCounters`, `ServiceCounters`, `DailyMetrics`, or `SimulationResult` — every one of V1's cost formulas, validation codes, and output shapes is untouched by V2.

## V2.5 Configuration contracts

### `configs/scenarios/*.yaml`: shock templates

`ShockConfig` (`data_io/loaders.py`) gains V2.3.3's distribution fields and V2.3.4's grouping field, replacing the three V1 fixed-day fields. V1's `physical_start_day`/`physical_end_day`/`information_day` are removed from the *config* schema (they remain exactly as-is on the realized, domain-level `Shock` — V2.4 — since that object is built by realization, not loaded from YAML):

```yaml
schema_version: 1
scenario_id: demand_spike_before_peak_season
description: >
  A realistic demand-side shock: a temporary 40% demand spike with an
  uncertain, jittered start day and a duration that isn't known in advance.

shocks:
  - shock_id: seasonal_demand_spike
    shock_type: DEMAND_SPIKE
    target_type: DEMAND
    target_id: plant_1
    planned_start_day: 25
    start_day_jitter_days: 3
    minimum_duration_days: 5
    duration_mean_days: 10
    duration_std_days: 3
    maximum_duration_days: 18
    max_information_delay_days: 2
    demand_multiplier: 1.4
    event_group_id: null
```

A **supplier-side** example, showing `SUPPLIER_CAPACITY_REDUCTION` and the reuse of `capacity_multiplier`:

```yaml
schema_version: 1
scenario_id: supplier_capacity_shortfall
description: Supplier's own release capacity is cut by 60% for roughly two weeks, with genuine timing and duration uncertainty.

shocks:
  - shock_id: supplier_output_shortfall
    shock_type: SUPPLIER_CAPACITY_REDUCTION
    target_type: NODE
    target_id: supplier_1
    planned_start_day: 21
    start_day_jitter_days: 4
    minimum_duration_days: 7
    duration_mean_days: 14
    duration_std_days: 4
    maximum_duration_days: 25
    max_information_delay_days: 3
    capacity_multiplier: 0.4
    event_group_id: null
```

A **compound/cascading** example, showing `event_group_id` correlating two shocks of different types under one root cause:

```yaml
schema_version: 1
scenario_id: regional_disruption_event
description: >
  One regional event with two correlated effects: the alternate port closes,
  and the supplier's output is simultaneously reduced by transport
  disruption in the same region. Both start together; each recovers and is
  disclosed on its own timeline.

shocks:
  - shock_id: regional_port_closure
    shock_type: EDGE_CLOSURE
    target_type: EDGE
    target_id: alternative_port_to_hub
    planned_start_day: 22
    start_day_jitter_days: 2
    minimum_duration_days: 5
    duration_mean_days: 9
    duration_std_days: 2
    maximum_duration_days: 15
    max_information_delay_days: 1
    event_group_id: regional_event_2026_a

  - shock_id: regional_supplier_shortfall
    shock_type: SUPPLIER_CAPACITY_REDUCTION
    target_type: NODE
    target_id: supplier_1
    planned_start_day: 22
    start_day_jitter_days: 2
    minimum_duration_days: 4
    duration_mean_days: 7
    duration_std_days: 2
    maximum_duration_days: 12
    max_information_delay_days: 3
    capacity_multiplier: 0.65
    event_group_id: regional_event_2026_a
```

(`regional_port_closure` and `regional_supplier_shortfall` share one `start_day_jitter` draw per V2.3.4, but realize independent durations and information delays.)

**Updated validation** (`ResolvedConfig`, `data_io/loaders.py`): V1's rule "every shock starts after warm-up" (V1 §12.5) becomes a worst-case check against jitter: `planned_start_day - start_day_jitter_days > warmup_days` must hold for every template, so that **no possible realization** of the shock can start during or before warm-up. Every other V1 cross-reference validation (target exists in the network, no duplicate `shock_id`s, etc.) is unchanged in kind, just applied to templates instead of fixed shocks.

### `configs/networks/*.yaml`: replenishment plan

`ReplenishmentPlanConfig` gains V2.3.5's quantity-distribution fields in place of the fixed `shipment_quantity`:

```yaml
replenishment_plan:
  product_id: component_a
  origin_node_id: supplier_1
  destination_node_id: plant_1
  first_release_day: 1
  release_every_days: 1
  shipment_quantity_mean: 40
  shipment_quantity_std: 6
  minimum_shipment_quantity: 20
  maximum_shipment_quantity: 55
  due_offset_days: 15
  initial_route_edge_ids:
    - supplier_to_primary_port
    - primary_port_to_hub
    - hub_to_plant
```

Setting `shipment_quantity_std: 0` reproduces V1's fixed-quantity behavior exactly (`shipment_quantity_mean` takes the role of V1's `shipment_quantity`).

### `configs/experiments/*.yaml`: the grid is config, not a new mechanism

**V2 introduces no new experiment-orchestration abstraction.** The 3×3 (topology × severity) grid (V2.8) is realized as **nine separate experiment config files**, each in exactly V1's existing `ExperimentConfig` shape (V1 §12.5) — only `network_config` and `scenario_config` differ between them — each run independently via the already-built `python -m supply_chain_simulator.cli run --config ...`. This is the exact operational pattern already validated for V1's own three-profile run (`baseline_comparison.yaml` / `light_disruption_comparison.yaml` / `heavy_disruption_comparison.yaml`): nine files instead of three, nothing else different. `cli.py`, `experiments/runner.py`, and `data_io/writers.py` require **no changes** to support the grid.

## V2.6 Repository structure additions

Purely additive to V1 §7's tree — no existing file moves, and V1's `baseline_network.yaml` / `port_closure.yaml` / `port_partial_capacity.yaml` / `port_extended_closure.yaml` / `baseline_comparison.yaml` / `light_disruption_comparison.yaml` / `heavy_disruption_comparison.yaml` stay exactly where they are, exactly as they are:

```text
configs/
├── networks/
│   ├── baseline_network.yaml          (V1, unchanged — the Standard topology tier)
│   ├── topology_compact.yaml          (NEW — V2.3.1)
│   └── topology_extended.yaml         (NEW — V2.3.1)
├── scenarios/
│   ├── port_closure.yaml              (V1, unchanged)
│   ├── port_partial_capacity.yaml     (V1, unchanged)
│   ├── port_extended_closure.yaml     (V1, unchanged)
│   ├── demand_spike_before_peak_season.yaml     (NEW — example, V2.5)
│   ├── supplier_capacity_shortfall.yaml         (NEW — example, V2.5)
│   └── regional_disruption_event.yaml           (NEW — example, V2.5)
└── experiments/
    ├── baseline_comparison.yaml       (V1, unchanged — this is the Standard x Medium grid cell)
    ├── light_disruption_comparison.yaml    (V1, unchanged — Standard x Light)
    ├── heavy_disruption_comparison.yaml    (V1, unchanged — Standard x Heavy)
    └── (six more, one per remaining grid cell — V2.8)
```

No new top-level architectural folder is introduced (`src/supply_chain_simulator`'s package boundaries are unchanged, per V1 §7's "do not create additional architectural folders" and V2.2's "what must not change"). `analysis/` (built during V1's results-visualization work, outside the core package) gains new capability, not a new folder — V2.7.

## V2.7 File-by-file implementation contract deltas

This section lists every existing V1 file that changes for V2, and exactly what changes in it. Any file not listed here is unchanged. As in V1 §11, "unchanged" means unchanged in public contract and behavior, not merely untouched by an editor.

### `domain/state.py`

- `OperationalNodeState` gains `source_capacity_multiplier: float = 1.0` (V2.4).
- `SimulationState` gains `pending_releases: list[ShipmentReleaseEvent]` (V2.4), initialized empty at day-0 construction (V1 §13.1).

### `domain/events.py`

- `ShockType` gains `DEMAND_SPIKE`, `DEMAND_DROP`, `SUPPLIER_CAPACITY_REDUCTION` (V2.3.2).
- `TargetType` gains `DEMAND` (V2.3.2).
- `Shock` gains `demand_multiplier: float = 1.0` and `event_group_id: str | None = None` (V2.3.2, V2.3.4). All other `Shock` fields are unchanged — a realized `Shock` still carries concrete `physical_start_day`/`physical_end_day`/`information_day` integers; only the *config* that produces it (V2.5) now expresses these as distributions.

### `experiments/event_tape.py`

The only file that gains genuinely new logic, since V1 §17.1's "all randomness drawn upfront, before any policy decision" is preserved by doing every new draw here, before `simulation/engine.py` ever runs.

New required functions, added alongside V1 §11.17's existing ones:

```python
realize_shock(template, stream_seed, shipment...) -> Shock
realize_shock_group(templates, stream_seed) -> list[Shock]
realize_release_quantity(mean, std, minimum, maximum, stream_seed, day, sequence) -> int
```

- `realize_shock` draws `start_day_jitter`, `duration`, and `information_delay` per V2.3.3's exact formulas and fixed order (jitter, then duration, then information delay), from the new `shock_realization` stream (V2.3.6).
- `realize_shock_group` implements V2.3.4: draws one shared jitter value for all templates sharing an `event_group_id`, then calls the per-member duration/information-delay draws independently, in `shock_id` ascending order.
- `realize_release_quantity` implements V2.3.5's TruncatedNormal-and-clamp pattern, called once per scheduled release in `generate_shipment_release_events`, from the new `release_quantity` stream, in day-ascending / sequence-ascending order (matching V1 §11.17's existing draw-order discipline).
- `generate_demand_events` gains one additional argument: the realized list of `DEMAND`-type shocks active on each day, used only to select which multiplier (if any) scales that day's `mean_daily_demand` before the existing `round(rng.gauss(...))` clamp step. This changes demand's *parameters* on shocked days; it adds no new draw and does not change the `demand` stream's draw count or order (V2.3.6) — the undisrupted tape still reuses the exact same `demand` stream draws with the multiplier removed.
- `build_disrupted_event_tape` and `build_undisrupted_event_tape` both gain a shock-realization step before demand generation (since realized `DEMAND`-type shocks feed into demand generation) and otherwise keep V1 §11.17's structure; the undisrupted tape's existing rule ("remove only the designed shock, keep every other draw identical") extends unchanged to every new shock type, per V2.3.6.

### `simulation/transition.py`

- `release_shipments` (V1 §11.8): where V1 raises `SimulationInvariantError` when a scheduled release exceeds source availability/capacity/storage, V2 instead appends the release event to `state.pending_releases` and continues (V2.3.7). `SimulationInvariantError` is still raised for genuine invariant violations elsewhere in the file; this is the one narrow, deliberate exception, justified by SUPPLIER_CLOSURE/SUPPLIER_CAPACITY_REDUCTION making an unreleasable day a normal, expected occurrence rather than a bug.
- `apply_shock_operational_state` gains one new branch: `SUPPLIER_CAPACITY_REDUCTION` sets `source_capacity_multiplier` on the target node's `OperationalNodeState` (mirroring the existing `NODE_CAPACITY_REDUCTION` → `processing_capacity_multiplier` branch exactly). `DEMAND_SPIKE`/`DEMAND_DROP` shocks are **not** handled here at all — per V2.3.3, demand shocks are realized into event-tape parameters at build time, never into runtime operational state, so this function's demand-related surface area is unchanged.
- Step 5 of the daily loop (`release_shipments`'s caller) changes per V2.3.7: attempt every entry in `state.pending_releases` (in shipment-ID order) before today's newly scheduled releases, each subject to the same availability/capacity/storage check; a still-infeasible entry stays in `pending_releases` for the next day.

### `data_io/loaders.py`

- `ScenarioConfig`'s nested shock schema replaces V1's three fixed-day fields with V2.5's distribution fields and `event_group_id`.
- `NetworkConfig`'s replenishment-plan schema replaces V1's fixed `shipment_quantity` with V2.5's mean/std/min/max fields.
- The warm-up-vs-shock-start validator changes from checking `physical_start_day > warmup_days` to checking `planned_start_day - start_day_jitter_days > warmup_days` (V2.5).
- No change to path resolution, secret handling, or any other V1 §11.20 rule.

### `analysis/plot_results.py`

Gains the ability to accept a 2D (topology × severity) set of experiment output directories — e.g. `--experiment <dir> --topology Compact --severity Light` repeated for each grid cell — and produce the "disruption robustness heatmap" plot type that V1's `VISUAL_REPORTING_PLAN_ESSENTIAL.md` specified but that V1's flat 3-profile grid couldn't populate (only one topology existed). The existing per-experiment and cross-severity plots (built for V1) are unchanged; the heatmap is additive. Because `analysis/` sits outside `src/supply_chain_simulator/` and outside mypy/pytest scope (V1 §8.4, confirmed unchanged by V2.2), this addition carries no new testing or type-checking obligation beyond what V1 already established for this script.

### Everything else

`simulation/routing.py`, `simulation/costs.py`, `simulation/engine.py`, `decisions/observation.py`, `decisions/validator.py`, `policies/*.py`, `integrations/llm_client.py`, `data_io/writers.py`, `cli.py` — **no changes**. V2's new randomness and new shock types resolve entirely to ordinary, already-supported domain objects (a realized `Shock`, a `ShipmentReleaseEvent` with a different quantity, a node's `source_capacity_multiplier`) before reaching any of these files; none of them can tell a V2-realized `Shock` apart from a V1 fixed one.

## V2.8 Experimental design: the topology × severity grid

### V2.8.1 The grid

V2 crosses two independent axes, per Yassine's confirmed direction (`Independent, crossed axes`):

|  | Light | Medium | Heavy |
|---|---|---|---|
| **Compact** | Compact × Light | Compact × Medium | Compact × Heavy |
| **Standard** | Standard × Light | Standard × Medium (= V1's `baseline_comparison.yaml`, unmodified) | Standard × Heavy |
| **Extended** | Extended × Light | Extended × Medium | Extended × Heavy |

Nine cells. Topology (Compact/Standard/Extended, V2.3.1) and severity (Light/Medium/Heavy, already built and validated in V1 as `port_partial_capacity.yaml`/`port_closure.yaml`/`port_extended_closure.yaml`) are deliberately named with disjoint vocabulary so a cell label is unambiguous without a legend.

Each cell is one `configs/experiments/*.yaml` file (V2.5) — nine files total, following V1's existing naming convention, e.g. `compact_light_comparison.yaml`, `extended_heavy_comparison.yaml`. `Standard × Medium` is exactly V1's existing `baseline_comparison.yaml` and is not duplicated.

Severity scenario files (`port_partial_capacity.yaml` etc.) are reused across topology tiers unchanged where their `target_id` exists in every tier (`port_primary` and `supplier_1` exist in Compact, Standard, and Extended by construction, V2.3.1); Extended's richer topology additionally supports the demand-side and supplier-side example scenarios from V2.5, which may be substituted into any tier's severity column at Yassine's discretion when the grid is populated — the grid's structure does not require every cell to use an identical shock *type*, only a comparable shock *severity*, consistent with V2.1's mission delta.

### V2.8.2 Replication count

V1 used 100 replications per single profile (300 total) within a $16.71 budget, calibrated empirically (V1's real 3-replication-per-profile calibration run) at roughly $0.05–$0.06 per replication at full horizon/drain. Nine cells at the same per-replication cost and the same total budget implies roughly 33 replications per cell — a real reduction in per-cell statistical power versus V1's single-profile runs.

This is a cost/power tradeoff, not a scientific rule, so it is **not locked** the way V1's fixed values were. The recommended default is:

```text
replications: 50   # per grid cell, 450 total
```

chosen as a middle point that keeps total spend near V1's original budget scale (450 × ~$0.055 ≈ $25) while giving each cell meaningfully more power than a bare 33. Yassine must explicitly confirm the actual `replications` value and run the same calibration procedure V1 used (a 3-replication real smoke at full horizon/drain per cell, or one representative cell if cells are judged cost-homogeneous) before committing to the full grid — this mirrors V1 §37's own "no coding agent may silently pick a scientific parameter" discipline, extended to V2's new grid.

## V2.9 Testing contract additions

All V1 tests (V1 §30) continue to pass unmodified — V2 adds tests, it does not rewrite V1's. New tests, following V1's existing per-module file convention:

### `tests/unit/test_event_tape.py` (additions)

- `realize_shock` is deterministic for a fixed stream seed and produces `start_day_jitter`/`duration`/`information_delay` draws in the fixed order specified by V2.3.3;
- a template with every uncertainty field zeroed (`start_day_jitter_days=0`, `duration_std_days=0`, `max_information_delay_days=0`) reproduces V1's exact fixed-shock behavior bit-for-bit, proving V2 is a strict superset of V1;
- `realize_shock_group` draws one shared jitter value across all members of an `event_group_id` and independent duration/information-delay per member;
- `realize_release_quantity` is deterministic, clamped to `[minimum_shipment_quantity, maximum_shipment_quantity]`, and reproduces V1's fixed quantity when `shipment_quantity_std=0`;
- the new `shock_realization` and `release_quantity` streams are distinct from `demand` and `edge_delays` and from each other (V2.3.6);
- demand-shock realization changes `generate_demand_events`'s *parameters* on shocked days without changing the `demand` stream's draw count or order versus an otherwise-identical unshocked run.

### `tests/unit/test_transition.py` (additions)

- a release that fails the source availability/capacity/storage check is appended to `pending_releases` instead of raising `SimulationInvariantError`;
- a pending release is retried, in shipment-ID order, before the current day's new releases, and succeeds once the source recovers;
- `SUPPLIER_CAPACITY_REDUCTION` reduces `source_capacity_multiplier` and correctly throttles `release_shipments`, mirroring the existing `NODE_CAPACITY_REDUCTION` test for `processing_capacity_multiplier`;
- `DEMAND_SPIKE`/`DEMAND_DROP` shocks produce **no** operational-state change (asserting the negative, since V2.3.2 deliberately keeps demand shocks out of runtime state).

### `tests/unit/test_loaders.py` (additions)

- `topology_compact.yaml` and `topology_extended.yaml` load into the exact node/edge counts and values specified in V2.3.1;
- a shock template config with `planned_start_day - start_day_jitter_days <= warmup_days` fails loudly (the updated V2.5 validator);
- a shock template with every V1-style fixed field but no new distribution fields is rejected (config schema is not silently backward-compatible — V1's scenario files must be migrated to the new schema, per V2.2's explicit non-goal of a compatibility shim);
- the three example scenario configs from V2.5 (demand spike, supplier shortfall, compound event) load into the exact expected `ShockConfig` objects, including correct `event_group_id` grouping.

### New: `tests/unit/test_topology.py`

- each topology tier's `NetworkDefinition` has the exact node/edge count and connectivity from V2.3.1;
- every tier's default `initial_route_edge_ids` begins at the source and ends at the destination (reusing V1's existing route-continuity check, applied to all three tiers);
- Compact has no structurally valid non-emergency reroute path around a `port_primary` closure (proving the intended REROUTE-impossible property);
- Extended has at least two structurally distinct non-emergency reroute paths around a `port_primary` closure.

### `tests/integration/test_paired_experiment.py` (additions)

- a compound event's two shocks realize a shared start day but independent durations, and the undisrupted tape removes both while preserving every other draw, extending V1's existing "undisrupted tape differs only by shocks" assertion to grouped shocks;
- a full paired run on each topology tier completes and produces valid TCD/delta values, proving no topology-specific code path is missing.

### `tests/integration/test_reproducibility.py` (additions)

- identical config and seed reproduce identical realized shocks (start day, duration, information day) and identical realized release quantities, extending V1's existing event-tape reproducibility assertion.

## V2.10 Milestones for V2

Building on V1's completed Milestones 0–9 (V1 §32):

### Milestone 10 — Domain and config deltas

Implement V2.4's dataclass changes and V2.5's config schema changes. Done when: `topology_compact.yaml`/`topology_extended.yaml` and the three example scenario configs load into exact expected objects; a V1-shaped scenario file is rejected with a precise schema error (V2.2's explicit no-compatibility-shim decision); all `test_loaders.py` additions pass.

### Milestone 11 — Shock and release realization

Implement `experiments/event_tape.py`'s new functions (V2.7). Done when: `realize_shock`, `realize_shock_group`, and `realize_release_quantity` are deterministic and tested; an all-zero-uncertainty template reproduces V1 bit-for-bit; the new streams are proven distinct; `test_event_tape.py` additions pass.

### Milestone 12 — Deferred releases and new shock effects

Implement `simulation/transition.py`'s deferral logic and the `SUPPLIER_CAPACITY_REDUCTION` operational-state branch (V2.7). Done when: a source-infeasible release defers instead of raising; a deferred release retries and succeeds; `SUPPLIER_CAPACITY_REDUCTION` throttles releases; demand shocks are confirmed to touch no operational state; `test_transition.py` additions pass.

### Milestone 13 — Topology tiers

Build `topology_compact.yaml` and `topology_extended.yaml` exactly per V2.3.1's tables. Done when: `test_topology.py` passes in full, including the Compact-has-no-reroute and Extended-has-multiple-reroutes structural proofs.

### Milestone 14 — Grid experiment configs and end-to-end run

Write the nine `configs/experiments/*.yaml` grid cells (V2.8.1), run `validate-config` on all nine, then run a 3-replication real calibration per V2.8.2 to measure actual per-cell OpenAI spend at full horizon/drain before committing to the full grid. Done when: all nine configs validate; calibration produces a real, measured per-replication cost; `test_paired_experiment.py`'s per-tier full-run addition passes for at least one cell per topology tier.

### Milestone 15 — Analysis grid heatmap

Extend `analysis/plot_results.py` to accept the 2D grid and produce the disruption-robustness heatmap (V2.7). Done when: run against real Milestone-14 calibration output, the heatmap renders correctly with no errors, for at least a partial (non-9-cell) grid.

### Milestone 16 — Full V2 grid execution

Run the full, Yassine-confirmed `replications`-per-cell grid (V2.8.2) end to end, all nine cells, `fail_fast=true` per cell as in V1. Done when: all nine cells complete; `summary.json` exists for each; the cross-cell heatmap and V1's existing per-experiment plots (V1's `analysis/plot_results.py` base functionality, unchanged) both render on the real full-grid output.

Do not start a milestone whose required predecessor is failing, per V1 §32's unchanged discipline.

## V2.11 Acceptance criteria for V2

V2 is complete only when all are true, additively to V1 §33 (which remains fully binding — V2 must never regress a V1 acceptance criterion):

### Architecture

- every V2.6 file/folder addition exists; no unapproved architectural folder was introduced; V2.7's file list is exhaustive (no undocumented file changed).

### Simulation

- Compact, Standard, and Extended topologies all load and run full paired experiments without error;
- an all-zero-uncertainty V2 shock template reproduces the corresponding V1 fixed-shock run's costs and metrics bit-for-bit (the strict-superset proof from V2.9);
- deferred releases correctly resolve once source capacity/availability recovers, and never silently drop a scheduled shipment;
- `DEMAND_SPIKE`/`DEMAND_DROP`/`SUPPLIER_CAPACITY_REDUCTION` all produce measurable, correctly-directioned effects on demand/backlog or release throughput respectively;
- compound events realize correlated start days and independent durations/information delays, and the undisrupted counterfactual removes every member of the group.

### Fairness

- every new randomness source draws from its own named stream, upfront, before any policy decision, per V1's unchanged §17.1/§20 principle;
- the undisrupted tape still differs from the disrupted tape only in shocks (now including grouped and demand-side shocks) and `newly_known_shock_ids`;
- both policies still see identical observations built from identical shared state, unchanged by any V2 addition;
- no V2 change touches `simulation/routing.py`, `decisions/validator.py`, or the LLM's five approved tools (V2.2's explicit constraint), confirmed by the V2.7 file-change list being exhaustive.

### Grid and metrics

- all nine grid cells validate and, once Yassine confirms a replication count, run to completion;
- the disruption-robustness heatmap (deferred from V1) renders correctly across the full grid;
- V1's existing metrics (TCD, delta, win rates, CI) are calculated identically per cell, with no formula change.

### Quality

- all V1 tests plus all V2.9 additions pass; Ruff and Mypy pass for all touched `src/` files; coverage remains at least 90%; no real API call occurs in any automated test; no secret exists in repository or outputs — every quality bar from V1 §33 holds for V2's additions too.

## V2.12 Final locked decisions for Version 2

| Topic | Version 2 decision |
|---|---|
| Topology | Three tiers — Compact, Standard, Extended — crossed independently with severity (V2.3.1, V2.8.1) |
| Severity | Unchanged from V1 — Light, Medium, Heavy (V1's existing three scenario files) |
| Grid mechanism | Nine flat experiment config files; no new orchestration abstraction (V2.5) |
| Disruption duration | Uncertain — sampled TruncatedNormal per replication (V2.3.3) |
| Disruption start day | Jittered around a planned day per replication (V2.3.3) |
| Disruption information | Delayed by an uncertain number of days after physical start (V2.3.3) |
| Shipment release timing | Unchanged from V1 — fixed schedule (V2.3.5) |
| Shipment release quantity | Uncertain — sampled TruncatedNormal per release (V2.3.5) |
| New shock types | DEMAND_SPIKE, DEMAND_DROP, SUPPLIER_CAPACITY_REDUCTION (V2.3.2) |
| Demand shock mechanism | Realized into event-tape generation parameters, never into runtime operational state (V2.3.2, V2.3.3) |
| Supplier closure | Reuses existing NODE_CLOSURE targeting a SUPPLIER node; no new enum value (V2.3.2) |
| Compound events | Correlated start-day jitter via `event_group_id`; independent duration and information delay per member (V2.3.4) |
| Infeasible releases | Deferred via `pending_releases`, retried daily, never an invariant error (V2.3.7) |
| New randomness streams | `shock_realization`, `release_quantity`, added to V1's `demand`/`edge_delays` (V2.3.6) |
| V1 scenario file compatibility | Not preserved — V1-shaped scenario configs must be migrated to the new schema; no compatibility shim (V2.2, V2.9) |
| Action space, validator, LLM tools, cost model, daily event order (except step 5) | Unchanged from V1 (V2.2) |
| Replications per grid cell | Not locked — Yassine confirms after a real per-cell calibration run, recommended default 50 (V2.8.2) |
| Document structure | Two-part: V1 frozen as Part 1, V2 as an additive Part 2 (this file) |

No coding agent may change these decisions without explicit approval from Yassine.

No coding agent may change these decisions without explicit approval from Yassine.
